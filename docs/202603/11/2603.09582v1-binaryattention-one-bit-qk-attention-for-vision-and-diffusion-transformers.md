---
title: "BinaryAttention: One-Bit QK-Attention for Vision and Diffusion Transformers"
title_zh: BinaryAttention：面向视觉与扩散 Transformer 的 1 比特 QK 注意力机制
authors: "Chaodong Xiao, Zhengqiang Zhang, Lei Zhang"
date: 2026-03-10
pdf: "https://arxiv.org/pdf/2603.09582v1"
tags: ["query:llm-cp"]
score: 9.0
evidence: 注意力模块的1比特量化
tldr: 针对Transformer注意力机制计算开销大的问题，本文提出BinaryAttention，通过将Query和Key量化为1比特并利用位运算替代浮点点积，显著提升了计算效率。该方法引入可学习偏置和自蒸馏技术以弥补精度损失，在ViT和DiT任务中实现了与全精度相当甚至更优的性能，且在A100上比FlashAttention2快2倍以上。
selection_source: fresh_fetch
motivation: 传统的8位或4位量化仍难以彻底解决Transformer注意力模块在视觉任务中的计算瓶颈。
method: 通过仅保留Query和Key的符号位并结合可学习偏置，利用位运算加速计算，并采用量化感知训练和自蒸馏确保精度。
result: 在视觉和扩散Transformer基准测试中，BinaryAttention的性能比肩全精度模型，且推理速度提升超过2倍。
conclusion: BinaryAttention为低比特视觉模型提供了一种高效且高精度的替代方案，推动了Transformer的高效部署。
---

## 摘要
Transformer 已取得广泛且显著的成功，但其注意力模块的计算复杂度仍是视觉任务中的主要瓶颈。现有方法主要采用 8 比特或 4 比特量化来平衡效率与准确性。在本文中，通过理论证明，我们指出注意力的二值化保留了核心的相似性关系，并提出了 BinaryAttention，这是一种用于快速且准确的 1 比特 QK 注意力的有效方法。具体而言，我们在计算注意力时仅保留查询（queries）和键（keys）的符号，并用位运算取代浮点点积，从而显著降低了计算成本。我们通过引入可学习偏置来缓解 1 比特量化下固有的信息损失，并实现了端到端加速。为了保持注意力的准确性，我们采用了量化感知训练和自蒸馏技术，在减轻量化误差的同时确保符号对齐的相似性。在 A100 GPU 上，BinaryAttention 的速度比 FlashAttention2 快 2 倍以上。在视觉 Transformer 和扩散 Transformer 基准测试上的大量实验表明，BinaryAttention 的性能与全精度注意力相当甚至更优，验证了其有效性。我们的工作为全精度注意力提供了一种高效且有效的替代方案，推动了低比特视觉和扩散 Transformer 的前沿发展。代码和模型可在 https://github.com/EdwardChasel/BinaryAttention 找到。

## Abstract
Transformers have achieved widespread and remarkable success, while the computational complexity of their attention modules remains a major bottleneck for vision tasks. Existing methods mainly employ 8-bit or 4-bit quantization to balance efficiency and accuracy. In this paper, with theoretical justification, we indicate that binarization of attention preserves the essential similarity relationships, and propose BinaryAttention, an effective method for fast and accurate 1-bit qk-attention. Specifically, we retain only the sign of queries and keys in computing the attention, and replace the floating dot products with bit-wise operations, significantly reducing the computational cost. We mitigate the inherent information loss under 1-bit quantization by incorporating a learnable bias, and enable end-to-end acceleration. To maintain the accuracy of attention, we adopt quantization-aware training and self-distillation techniques, mitigating quantization errors while ensuring sign-aligned similarity. BinaryAttention is more than 2x faster than FlashAttention2 on A100 GPUs. Extensive experiments on vision transformer and diffusion transformer benchmarks demonstrate that BinaryAttention matches or even exceeds full-precision attention, validating its effectiveness. Our work provides a highly efficient and effective alternative to full-precision attention, pushing the frontier of low-bit vision and diffusion transformers. The codes and models can be found at https://github.com/EdwardChasel/BinaryAttention.

---

## 论文详细总结（自动生成）

以下是对论文《BinaryAttention: One-Bit QK-Attention for Vision and Diffusion Transformers》的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
Transformer 模型在视觉和生成任务中表现卓越，但其核心的注意力机制（Attention）存在计算瓶颈：计算复杂度随序列长度呈平方级增长。虽然现有的量化技术（如 8-bit 或 4-bit）能一定程度上缓解压力，但进一步压缩至 1-bit（二值化）通常会导致严重的信息丢失和性能骤降。
**本文的核心目标**是探索 1-bit QK 注意力（即 Query 和 Key 均为 1 比特）在理论上的可行性，并开发一种既能实现硬件级加速，又能保持甚至超越全精度模型性能的高效方案。

### 2. 方法论：核心思想与关键技术
BinaryAttention 的核心思想是将注意力机制中最耗时的点积运算转化为超高效的位运算。
*   **理论证明**：论文通过数学证明（定理 1）指出，二值化后的 Query 和 Key 在统计上保留了原始数据的协方差结构，这意味着 1-bit 空间仍能有效捕捉 token 间的相似性关系。
*   **缩放二值表示**：通过公式 $s_i = \mu_q \text{sign}(q_i)$ 和 $t_j = \mu_k \text{sign}(k_j)$ 将 Q 和 K 量化为 1-bit，其中 $\mu$ 是均值缩放因子，用于减少量化误差。
*   **偏置增强（Bias Enhancement）**：1-bit 量化会导致注意力分布过于“平坦”。作者引入了一个可学习的偏置项 $b_{ij}$（如相对位置偏置），以重新引入上下文和结构信息，增强模型的判别力。
*   **混合量化方案**：为了端到端加速，Q 和 K 采用 1-bit，而注意力权重（P）和值（V）采用 8-bit 量化。
*   **硬件优化实现**：基于 FlashAttention2 的算子融合思想，利用 NVIDIA GPU 的 `mma.s32.b1.b1.s32` 指令（针对二值矩阵乘法）和 `mma.s32.u8.s8.s32` 指令（针对 8-bit 乘法）编写了定制化的 CUDA 内核。

### 3. 实验设计
论文在多个主流视觉任务上进行了广泛验证：
*   **图像分类**：在 ImageNet-1K 上测试了 DeiT-T/S/B 变体。
*   **目标检测与分割**：在 COCO 2017 数据集上使用 Mask R-CNN 和 Cascade Mask R-CNN。
*   **语义分割**：在 ADE20K 数据集上使用 UPerNet。
*   **图像生成**：在 ImageNet 256x256 上测试了扩散 Transformer（DiT）和 SiT。
*   **对比方法**：包括 FlashAttention2（全精度基准）、SageAttention（8-bit 注意力）、线性注意力模型（如 Hydra, EfficientVit）、状态空间模型（Vim）以及传统的 W8A8 量化方法（PTQ4ViT）。

### 4. 资源与算力
*   **硬件平台**：所有实验和算力测试均在 **NVIDIA A100 GPU** 上完成。
*   **训练细节**：分类任务遵循 DeiT 的标准设置（300 epochs）；生成任务中，小模型训练 200K 步，XL 变体训练达 4000K 步。
*   **效率表现**：在 A100 上，BinaryAttention 的内核速度比 FlashAttention2 快 **2 倍以上**，比 SageAttention 快约 1.4 倍。

### 5. 实验数量与充分性
作者进行了极其详尽的实验：
*   **多任务覆盖**：涵盖了从基础分类到复杂生成、从密集预测到长序列推理的四大类任务。
*   **消融实验**：针对缩放因子、偏置项、自蒸馏策略分别做了对比，验证了各组件的必要性。
*   **保真度分析**：通过余弦相似度、RMSE 等指标量化了 1-bit 注意力对原始注意力模式的还原程度。
*   **客观性**：实验对比了多种 SOTA 方案，并提供了端到端的吞吐量测试，而非仅仅停留在理论算力（FLOPs）的对比上，实验设计非常充分且具有说服力。

### 6. 主要结论与发现
*   **性能不降反升**：在多个任务中，BinaryAttention 匹配甚至超过了全精度基准。例如，DeiT-B 在 384 分辨率下比全精度模型高出 0.5% 的准确率。
*   **极速推理**：通过 1-bit QK 结合位运算，显著降低了计算延迟，尤其在高分辨率输入下优势更明显。
*   **生成质量高**：在 DiT 模型中，BinaryAttention 生成的图像在 FID 指标上与全精度模型持平，证明了其在生成式 AI 中的潜力。

### 7. 优点与亮点
*   **理论与实践结合**：不仅有严谨的数学证明支撑 1-bit 的合理性，还有深度优化的 CUDA 内核实现。
*   **即插即用**：该方法不需要改变 Transformer 的基本架构，可以作为现有模型的替代模块。
*   **协同效应**：展示了 BinaryAttention 可以与现有的线性层量化方法（如 PTQ4ViT）结合，进一步压缩模型。

### 8. 不足与局限
*   **量化不彻底**：虽然 QK 是 1-bit，但 Value (V) 仍保留在 8-bit，且 MLP 层未进行二值化，尚未达到全模型二值化的极致效率。
*   **训练成本**：该方法依赖量化感知训练（QAT）和自蒸馏，这意味着需要额外的微调过程，不像某些 PTQ 方法那样可以“拿来即用”。
*   **内存开销风险**：引入密集偏置项（Dense Bias）时，在高分辨率下会增加显存占用（虽然文中提出了分解偏置的变体来缓解）。

（完）
