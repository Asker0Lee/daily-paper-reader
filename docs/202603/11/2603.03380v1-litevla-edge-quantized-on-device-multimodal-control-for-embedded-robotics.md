---
title: "LiteVLA-Edge: Quantized On-Device Multimodal Control for Embedded Robotics"
title_zh: LiteVLA-Edge：面向嵌入式机器人的量化设备端多模态控制
authors: "Justin Williams, Kishor Datta Gupta, Roy George, Mrinmoy Sarkar"
date: 2026-03-03
pdf: "https://arxiv.org/pdf/2603.03380v1"
tags: ["query:llm-cp"]
score: 9.0
evidence: VLA模型的训练后4位量化
tldr: LiteVLA-Edge 针对嵌入式机器人部署，提出了一种高效的视觉-语言-动作（VLA）模型流水线。通过在 Jetson Orin 硬件上结合 FP32 监督微调、4位 GGUF 量化以及 llama.cpp 加速推理，实现了完全离线的端到端控制。该系统在 ROS 2 环境下达到了 6.6Hz 的推理频率，为在资源受限的嵌入式设备上运行多模态控制模型提供了可行的实践路径和基准。
selection_source: fresh_fetch
motivation: 现有的 VLA 模型计算需求高且推理延迟大，难以在嵌入式机器人硬件上实现实时、本地化的部署。
method: 结合 FP32 监督微调与 4-bit GGUF 量化技术，并利用 llama.cpp 运行时在 Jetson Orin 上进行 GPU 加速推理。
result: 在 ROS 2 集成环境下实现了 150.5ms 的平均端到端延迟（约 6.6Hz），支持完全离线的实时控制。
conclusion: 该研究验证了在嵌入式硬件上执行紧凑型多模态控制模型的技术可行性，并为端侧 VLA 的未来评估建立了基准。
---

## 摘要
视觉-语言-动作 (VLA) 模型为感知、语言调节和动作生成提供了一个统一的框架，但由于计算需求和推理延迟，许多现有系统仍难以部署在嵌入式机器人环境中。在本文中，我们提出了 LiteVLA-Edge，这是一个面向部署的 VLA 流水线，旨在 Jetson Orin 级硬件上实现完全的设备端推理。我们的方法结合了 FP32 精度下的监督式图像到动作微调、训练后 4-bit GGUF 量化，以及通过 llama.cpp 运行时的 GPU 加速推理。在我们的部署配置下，LiteVLA-Edge 在集成 ROS 2 的感知-推理-动作流水线中完全离线运行，实现了 150.5 ms（约 6.6 Hz）的平均端到端延迟。我们的贡献并非引入新的策略目标，而是提供了一条在嵌入式硬件上本地执行紧凑型多模态控制模型的实际系统路径，同时保留了感知、推理和执行之间的模块化接口。这些结果确立了反应式语言调节控制的时间可行性，并为未来机器人领域设备端 VLA 的任务级评估提供了可复现的基准。

## Abstract
Vision-Language-Action (VLA) models provide a unified framework for perception, language conditioning, and action generation, but many existing systems remain difficult to deploy in embedded robotic settings because of their computational requirements and inference latency. In this paper, we present LiteVLA-Edge, a deployment-oriented VLA pipeline for fully on-device inference on Jetson Orin-class hardware. Our approach combines supervised image-to-action fine-tuning in FP32 with post-training 4-bit GGUF quantization and GPU-accelerated inference through the \texttt{llama.cpp} runtime. Under our deployment configuration, LiteVLA-Edge achieves a mean end-to-end latency of 150.5\,ms (approximately 6.6\,Hz) while operating entirely offline within a ROS~2-integrated perception--reasoning--action pipeline. Rather than introducing a new policy objective, our contribution is a practical systems path for executing compact multimodal control models locally on embedded hardware while preserving modular interfaces between perception, reasoning, and actuation. These results establish timing feasibility for reactive language-conditioned control and provide a reproducible baseline for future task-level evaluation of on-device VLAs in robotics.

---

## 论文详细总结（自动生成）

### LiteVLA-Edge: 嵌入式机器人的量化设备端多模态控制总结

#### 1. 核心问题与研究动机
*   **核心问题**：现有的视觉-语言-动作（VLA）模型（如 OpenVLA、RT-2）参数量巨大（通常 >7B），依赖云端计算或高性能桌面 GPU，无法在功耗受限、低带宽或无 GPS 的嵌入式机器人环境（如 Jetson Orin、Raspberry Pi）中实现低延迟的本地实时控制。
*   **研究动机**：将 VLA 研究从“慢速推理”转向“实时视觉运动控制”。通过优化，在 40W 功率限制的边缘设备上实现足以支持闭环控制的推理频率（>6Hz），解决机器人因推理过慢导致的“走走停停”行为。

#### 2. 方法论
*   **核心思想**：采用“高精度微调 + 深度量化压缩 + 硬件加速推理”的流水线，将紧凑型多模态模型转化为实时的机器人策略。
*   **关键技术细节**：
    *   **模型骨干**：使用轻量级的 **SmolVLM-256M** 作为多模态 Transformer 基础。
    *   **监督微调 (SFT)**：在 FP32 精度下，利用 **LoRA**（秩 $r=8$，缩放因子 $\alpha=8$）进行图像到动作的映射训练，确保动作指令的精确性。
    *   **量化压缩**：采用训练后量化（PTQ），将模型转换为 **4-bit (Q4_K_M) GGUF** 格式，显著降低内存占用和总线延迟。
    *   **推理加速**：利用 **llama.cpp** 库及其 CUDA 后端，将 Transformer 的全部 42 层卸载到 Jetson AGX Orin 的 GPU 上。
    *   **系统集成**：通过 **ROS 2** 节点集成，将模型输出的 Token 解码为 `geometry_msgs/Twist` 速度指令，实现感知-推理-动作的闭环。

#### 3. 实验设计
*   **硬件环境**：主要在 **NVIDIA Jetson AGX Orin (64GB)** 上进行评估，并对比了 Raspberry Pi 4 的表现。
*   **测试场景**：模拟闭环感知-动作流水线，输入连续 RGB 帧，测量从图像摄取到动作输出的端到端延迟。
*   **对比方法 (Benchmarks)**：
    *   **通用 VLM**：Moondream2, TinyLLaVA, PaliGemma, Qwen2-VL（评估其是否支持闭环控制）。
    *   **现有 VLA**：OpenVLA (7B), EdgeVLA, EfficientVLA（对比参数量、硬件需求和推理频率）。

#### 4. 资源与算力
*   **推理算力**：使用了 **NVIDIA Jetson AGX Orin**，在 40W 功率模式下运行。
*   **训练算力**：文中提到在 FP32 精度下进行 LoRA 微调，但**未明确说明**具体使用的训练 GPU 型号（如 A100 或 RTX 4090）及具体的训练时长。
*   **配置参数**：上下文窗口 $n_{ctx} = 512$，最大输出 Token 数限制为 12。

#### 5. 实验数量与充分性
*   **实验规模**：进行了 **300 次** 连续推理运行以测量统计学意义上的平均延迟、标准差和抖动。
*   **充分性评价**：
    *   **性能评估充分**：对于推理延迟、抖动（Jitter）和硬件利用率的测量非常详尽，证明了系统层面的可行性。
    *   **任务评估不足**：实验主要集中在“计时性能”上，缺乏在复杂、多样化真实机器人任务中的成功率（Success Rate）对比实验。
    *   **客观性**：作者明确指出其贡献在于“系统路径”而非“新算法”，对比分析相对客观，承认了小模型在推理深度上可能逊色于 7B 模型。

#### 6. 主要结论与发现
*   **延迟突破**：在 Jetson AGX Orin 上实现了 **150.5 ms** 的平均端到端延迟，推理频率达到 **6.64 Hz**，比之前的基准提升了约 220%。
*   **稳定性**：系统表现出极低的抖动（标准差 $\sigma < 0.2 ms$），这对于维持 ROS 2 控制心跳的确定性至关重要。
*   **闭环可行性**：6.6 Hz 的频率跨越了“反应式控制”的门槛，允许机器人在运动过程中根据视觉反馈实时修正轨迹。

#### 7. 优点
*   **实用性强**：提供了一套完整的、基于开源工具（llama.cpp, ROS 2）的部署方案，易于复现。
*   **端侧优化**：通过 4-bit 量化解决了显存瓶颈，使 VLA 模型能完全运行在嵌入式 GPU 的统一内存中。
*   **模块化设计**：保留了感知、推理、执行的独立接口，方便安全覆盖（Safety Overrides）和调试。

#### 8. 不足与局限
*   **任务泛化性验证缺失**：论文未提供在 Open X-Embodiment 等标准机器人数据集上的操作成功率数据，难以评估量化对复杂逻辑推理的具体影响。
*   **模型容量限制**：256M 的参数量虽然快，但在处理需要复杂常识推理或长程规划的任务时，性能可能远不如 7B 规模的模型。
*   **应用范围**：目前仅验证了速度矢量（Twist）输出，对于需要高维动作空间（如 7 自由度机械臂抓取）的适用性尚待证明。

（完）
