---
title: Practical FP4 Training for Large-Scale MoE Models on Hopper GPUs
title_zh: Hopper GPU 上大规模 MoE 模型的实用 FP4 训练
authors: "Wuyue Zhang, Chongdong Huang, Chunbo You, Cheng Gu, Fengjuan Wang, Mou Sun"
date: 2026-03-03
pdf: "https://arxiv.org/pdf/2603.02731v1"
tags: ["query:llm-cp"]
score: 9.0
evidence: 大规模模型的FP4训练与量化
tldr: 针对Hopper架构GPU缺乏原生FP4支持的问题，本文提出了一种适用于大规模混合专家（MoE）模型的FP4训练方案。通过引入FP8与FP4之间的直接量化转换及缩放感知转换技术，在不损失收敛性的前提下，实现了激活值和专家并行通信的MXFP4压缩。在671B参数规模下，该方法显著降低了内存占用并提升了训练吞吐量，证明了软硬件协同设计在现有硬件上实现低比特训练的可行性。
selection_source: fresh_fetch
motivation: 大规模MoE模型训练受限于激活内存和通信带宽，且Hopper GPU缺乏原生FP4硬件支持。
method: 提出一种将FP4集成到BF16/FP8混合流水线中的方案，通过FP8与FP4直接转换及缩放感知转换，实现激活值和通信的MXFP4压缩。
result: "在671B参数模型上，峰值激活内存减少14.8%，训练吞吐量提升12.5%，且保持了与FP8基准相当的训练性能。"
conclusion: 通过精细的软硬件协同设计，即使在缺乏原生硬件支持的情况下，也能在Hopper GPU上实现高效的FP4大规模MoE训练。
---

## 摘要
大规模混合专家（MoE）模型的训练受限于激活内存和专家并行通信，然而在缺乏原生 MXFP4 或 NVFP4 支持的 Hopper 级 GPU 上，FP4 训练仍然难以实现。在这项工作中，我们提出了一种训练方案，使 MoE 模型在没有原生 4 位计算支持的 Hopper 架构上也能实现 MXFP4 级别的效率。核心挑战在于如何将 FP4 集成到现有的 BF16/FP8 混合训练流水线中，而不产生昂贵的精度转换开销（例如 FP4 ↔ BF16 ↔ FP8）。我们通过引入直接的 FP8 到 FP4 量化与反量化，以及感知缩放的 FP4 行转列转换，解决了这一挑战，从而以极低的开销实现了 FP4 激活和专家并行通信。核心 MoE 计算以 FP8 执行，而激活和专家并行通信则使用 MXFP4 进行压缩，在不降低收敛性的情况下实现了显著的内存和带宽节省。在 671B 参数规模下，我们的方法实现了与强 FP8 基线相当的端到端训练性能，同时将峰值激活内存降低了 14.8% (11.8 GB)，并将训练吞吐量提高了 12.5%（从每 GPU 每秒 1157 个 token 增加到 1302 个）。这些结果表明，通过精细的软硬件协同设计，即使在没有原生 FP4 Tensor Core 支持的情况下，大规模 MoE 训练也能实际获得 FP4 的效率。

## Abstract
Training large-scale Mixture-of-Experts (MoE) models is bottlenecked by activation memory and expert-parallel communication, yet FP4 training remains impractical on Hopper-class GPUs without native MXFP4 or NVFP4 support. In this work, we present a training recipe that enables MXFP4 efficiency for MoE models on Hopper architectures without native 4-bit computation support. A central challenge is to integrate FP4 into an existing BF16/FP8 hybrid training pipeline without incurring costly precision round-trips (e.g., FP4 $\leftrightarrow$ BF16 $\leftrightarrow$ FP8). We address this challenge by introducing direct FP8-to-FP4 quantization and de-quantization, together with scaling-aware FP4 row-wise to column-wise conversion, enabling FP4 activations and expert-parallel communication with minimal overhead. Core MoE computations are executed in FP8, while activations and expert-parallel communication are compressed using MXFP4, achieving substantial memory and bandwidth savings without degrading convergence. At the 671B parameter scale, our method achieves end-to-end training performance comparable to strong FP8 baselines, while reducing peak activation memory by 14.8\% (11.8 GB) and improving training throughput by 12.5\%, from 1157 to 1302 tokens per GPU per second. These results show that FP4 efficiency can be practically realized for large-scale MoE training through careful software-hardware co-design, even without native FP4 Tensor Core support.

---

## 论文详细总结（自动生成）

这是一份关于论文《Practical FP4 Training for Large-Scale MoE Models on Hopper GPUs》的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：大规模混合专家（MoE）模型在训练时面临严重的**激活内存瓶颈**和**专家并行（Expert-Parallel）通信带宽限制**。
*   **背景**：虽然新一代硬件（如 NVIDIA Blackwell）原生支持 FP4 精度，但当前主流的 **Hopper 架构（如 H100）缺乏原生 FP4 计算和通信指令**。
*   **动机**：在 Hopper GPU 上直接模拟 FP4 通常需要经过昂贵的中间格式转换（如 FP4 $\leftrightarrow$ BF16 $\leftrightarrow$ FP8），这会抵消低比特带来的性能增益。本文旨在通过软硬件协同设计，在不具备原生支持的 Hopper GPU 上实现实用的、高性能的 FP4 训练方案。

### 2. 论文提出的方法论
核心思想是**“计算与存储/通信精度解耦”**：使用 FP8 进行高强度的 GEMM 计算，而使用 MXFP4 进行激活值存储和跨 GPU 通信。
*   **混合精度流水线**：在前向传播中，激活值在 All-to-All 通信前被量化为 MXFP4，并以该格式缓存用于反向传播；在计算密集型算子中，再将其转回 FP8。
*   **直接位运算转换（Direct FP4-to-FP8）**：设计了一种跳过 BF16 中间层的算法。通过位移操作直接处理 MXFP4（E2M1 编码）和 FP8（E4M3 编码）的符号、指数和尾数，显著降低转换延迟。
*   **分层缩放对齐（Hierarchical Scaling Alignment）**：解决了 MXFP4（每 32 个元素一个缩放因子）与 FP8（每 128 个元素一个缩放因子）之间块粒度不匹配的问题，通过取最大值并补偿指数位来实现数值对齐。
*   **算子融合与布局优化**：开发了定制 CUDA Kernel（如 `FP4RowToFP8Col`），将反量化、矩阵转置和重构融合在一起，利用共享内存（Shared Memory）避免银行冲突（Bank Conflict），减少了 3 倍的全局内存访问。
*   **非对称策略**：在反向传播中主动回退到 FP8 通信，因为实验发现梯度量化为 FP4 的转换开销超过了通信节省的收益。

### 3. 实验设计
*   **模型对象**：671B 参数的大规模 MoE 模型（采用 MLA 架构，类似 DeepSeek-V3）以及 236B 的中型 MoE 模型。
*   **Benchmark（基准）**：
    1.  **BF16**：标准 bfloat16 混合精度训练。
    2.  **FP8**：基于 Transformer Engine 的块缩放（Blockwise）FP8 优化方案。
*   **对比指标**：训练吞吐量（Tokens per GPU per Second, TGS）、峰值内存占用、收敛损失（Loss）曲线。

### 4. 资源与算力
*   **硬件环境**：使用了 32 个节点，共计 **256 张 NVIDIA Hopper GPU (80GB HBM3)**。
*   **互联技术**：节点内通过 NVSwitch 连接，节点间通过 InfiniBand 连接。
*   **训练时长**：文中未明确给出 671B 模型的总训练时长，但在收敛性验证部分，使用 16B 模型在 **160B tokens** 上进行了对比实验。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了从 16B（验证收敛性）到 236B 和 671B（验证性能）的多个量级。
*   **消融实验**：详细对比了算子融合前后的加速比（1.6x-1.9x）、不同重计算策略下的内存变化，以及量化带来的额外开销分析。
*   **充分性评价**：实验设计非常贴近工业界生产环境，直接在 671B 这种顶级规模模型上做测试，具有极高的说服力。对比基准选择了目前最强的 FP8 实现，而非弱基准，体现了实验的公平性和客观性。

### 6. 主要结论与发现
*   **内存节省**：在 671B 模型上，相比 FP8 基准，**峰值激活内存降低了 14.8% (11.8 GB)**。
*   **吞吐量提升**：由于内存压力减小，可以减少昂贵的“重计算（Recomputation）”操作。在相同硬件下，吞吐量从 1157 提升至 1302 TGS，**增幅达 12.5%**。
*   **收敛性**：MXFP4 的 Loss 曲线与 BF16 几乎重合，相对 Loss 偏差仅为 +0.61%，证明了该方案在生产环境中的稳定性。
*   **MoE 适配性**：定制的 `msplit` Kernel 在处理 MoE 专家层的分组内存访问时，比通用库快 1.4x-1.5x。

### 7. 优点
*   **极强的实用性**：为当前大量存算的 Hopper GPU 提供了升级 FP4 效率的路径，无需等待 Blackwell 硬件普及。
*   **精妙的 Kernel 设计**：通过位运算和分层缩放对齐，解决了异构低比特格式转换的性能损耗问题。
*   **系统级优化**：不仅关注计算，还深入优化了 DeepEP 通信库，实现了端到端的 FP4 流程。

### 8. 不足与局限
*   **非完全 FP4 训练**：反向传播仍依赖 FP8，未能实现全流程 FP4 通信，这反映了软件模拟在处理某些梯度模式时的局限。
*   **架构依赖**：该方案高度依赖 Hopper 架构的特性和 DeepSeek 开源生态（如 DeepEP, Transformer Engine），在其他厂商（如 AMD 或国产芯片）上的迁移成本未知。
*   **复杂度增加**：引入了复杂的位运算 Kernel 和非对称训练流，增加了训练框架的维护难度。

（完）
