---
title: High-Fidelity Pruning for Large Language Models
title_zh: 大语言模型的高保真剪枝
authors: "Yijun Zhu, Jianxin Wang, Chengchao Shen"
date: 2026-03-09
pdf: "https://arxiv.org/pdf/2603.08083v1"
tags: ["query:llm-cp"]
score: 10.0
evidence: 提出了一种基于重要性评估的LLM高保真剪枝方法
tldr: 针对大语言模型剪枝中传统泰勒展开准则仅依赖交叉熵损失、忽略模型全局预测分布的问题，本文提出了一种高保真剪枝方法 HFPrune。该方法创新性地利用模型输出分布的信息熵作为重要性评估准则，无需额外教师模型即可实现高效的神经元评估。实验证明，该方法在 LLaMA 和 Qwen 系列模型上均优于现有剪枝技术，有效保持了模型的预测性能。
selection_source: fresh_fetch
motivation: 传统的基于交叉熵的泰勒剪枝仅关注预测概率最高的 Token，忽略了模型输出分布的全局信息，且自蒸馏方法计算开销过大。
method: 提出使用模型输出分布的信息熵作为泰勒剪枝的重要性评估准则，以更全面地衡量神经元对模型预测能力的影响。
result: 在 LLaMA 和 Qwen 系列模型上的广泛零样本基准测试表明，该方法在保持模型保真度方面持续优于现有的剪枝方法。
conclusion: 通过引入信息熵准则，HFPrune 提供了一种无需教师模型、高效且高保真的 LLM 剪枝方案。
---

## 摘要
大语言模型（LLMs）在广泛的任务中展现了卓越的性能，但其巨大的计算和内存需求给部署带来了重大挑战。一种常见的方法是利用损失函数的泰勒展开（Taylor expansion）来估计神经元的重要性。然而，由于其依赖于 one-hot 交叉熵损失，一个关键的局限性是它仅根据分配给单个预测的下一个 token 的概率来狭隘地评估重要性，从而忽略了原始模型的其他潜在预测。解决这一问题的一个直观方案是采用自蒸馏（self distillation）准则进行重要性评估。然而，这种方法需要一个单独的教师模型进行监督，从而引入了显著的计算开销。为此，我们提出了一种简单而有效的准则——模型输出分布的信息熵（information entropy），以便在不需要额外教师模型的情况下，利用泰勒剪枝高效地评估神经元的重要性得分。与普通的交叉熵准则相比，它为泰勒剪枝提供了一个更全面的准则，以全局方式剪掉对模型预测影响最小的神经元，从而保持了模型预测能力的高保真度。在广泛的零样本（zero-shot）基准测试上的实验结果表明，我们的方法在 LLaMA 和 Qwen 系列模型上始终优于现有的剪枝方法。源代码和训练权重可在 https://github.com/visresearch/HFPrune 获取。

## Abstract
Large Language Models (LLMs) have demonstrated exceptional performance across a wide range of tasks, yet their significant computational and memory requirements present major challenges for deployment. A common approach uses Taylor expansion on the loss function to estimate neuron importance. However, its reliance on one-hot cross entropy loss, a key limitation is that it narrowly assesses importance based only on the probability assigned to the single predicted next token, thereby ignoring the other potential predictions of the original model. An intuitive solution to address this is to employ self distillation criterion for importance evaluation. However, this approach introduces significant computational overhead by requiring a separate teacher model for supervision. To this end, we propose a simple but effective criterion, information entropy of the model's output distribution, to efficiently evaluate importance scores of neurons with Taylor pruning without requirement of additional teacher. Compared to plain cross entropy criterion, it provides a more holistic criterion for Taylor pruning to prune neurons with the least impact on the prediction of model in a global manner, thereby preserving the fidelity of the model's predictive capabilities. Experimental results on extensive zero-shot benchmarks demonstrate that our method consistently outperforms existing pruning methods across the LLaMA and Qwen series models. The source code and trained weights are availabel at https://github.com/visresearch/HFPrune.

---

## 论文详细总结（自动生成）

这是一份关于论文《High-Fidelity Pruning for Large Language Models》（大语言模型的高保真剪枝）的结构化深入总结：

### 1. 核心问题与研究动机
*   **研究背景**：大语言模型（LLMs）参数量巨大，部署成本极高。结构化剪枝是降低计算和内存开销的有效手段。
*   **核心问题**：现有的基于泰勒展开（Taylor expansion）的剪枝方法主要依赖**one-hot交叉熵损失**来评估神经元重要性。这种方式仅关注模型对“正确标签”的预测概率，忽略了输出分布中蕴含的其他潜在预测信息（即全局知识），导致剪枝后模型保真度受损。
*   **现有方案缺陷**：
    *   **交叉熵准则**：视角狭隘，无法捕捉模型完整的预测逻辑。
    *   **自蒸馏准则**：虽然考虑了分布，但需要额外的教师模型（开销大），且在初始阶段由于预测一致导致梯度为零，无法有效评分。

### 2. 方法论：HFPrune
*   **核心思想**：引入**信息熵（Information Entropy）**作为泰勒剪枝的重要性评估准则。通过衡量神经元对模型输出分布不确定性的贡献，实现“标签无关”且“全局感知”的重要性评分。
*   **关键技术细节**：
    1.  **目标模块**：重点针对LLM中参数占比最高（如Llama2-7B中占68.3%）的MLP（SwiGLU）模块进行神经元剪枝。
    2.  **重要性评分公式**：利用一阶泰勒展开，神经元 $h_i$ 的重要性 $I_i$ 定义为：$I_i = |\frac{\partial C_H(x)}{\partial h_i} h_i|$，其中 $C_H(x)$ 是模型输出分布的信息熵。
    3.  **算法流程**：
        *   在少量无标签校准数据上运行前向传播。
        *   计算输出分布的信息熵，并通过反向传播获取神经元梯度。
        *   根据重要性得分对神经元排序，剔除得分最低的固定比例（$\rho_{mlp}$）神经元。
        *   **性能恢复**：剪枝后进行简短的微调（使用LoRA技术）以恢复模型能力。

### 3. 实验设计
*   **模型对象**：涵盖 LLaMA-2-7B、LLaMA-3.2 (1.2B/3.2B)、Qwen-2.5 (1.5B/7B) 及 Qwen-3-1.7B。
*   **数据集**：
    *   **校准**：从 C4 数据集中随机抽取约 4.3 万条序列。
    *   **微调**：使用 LaMini-instruction 指令微调数据集。
*   **Benchmark**：在 10 个主流零样本（Zero-shot）基准测试上评估（如 ARC、BoolQ、PIQA、Race、TruthfulQA、WinoGrande 等）。
*   **对比方法**：LLM-Pruner、LoRAPrune、LoRAP、SDMPrune、Compresso 等。

### 4. 资源与算力
*   **硬件设备**：
    *   **7B 级模型**：使用 8 张 NVIDIA A6000 GPU 进行微调（Batch size 512）。
    *   **较小模型**：使用 4 张 NVIDIA RTX 4090 GPU（Batch size 256）。
*   **效率表现**：在 Llama2-7B 上，HFPrune 的剪枝评估过程比 SDMPrune 快约 3 倍，且峰值显存占用降低了 31%。
*   **推理加速**：剪枝 30% 参数后，Prefill 阶段实现 1.47 倍加速，解码吞吐量提升 35.8%。

### 5. 实验数量与充分性
*   **实验规模**：论文在 3 个不同系列、6 种不同规模的模型上进行了验证，涵盖了从 1.2B 到 7B 的参数区间。
*   **消融实验**：
    *   对比了不同剪枝准则（CE Loss vs. SD Loss vs. 信息熵）。
    *   验证了仅剪枝 MLP 与同时剪枝 Attention+MLP 的效果差异。
    *   通过 JS 散度和 Jaccard 相似度定量分析了输出分布的保真度。
*   **评价**：实验设计全面，跨模型、跨比例的对比论证了方法的稳健性，消融实验直接回应了核心假设，具有较高的客观性和说服力。

### 6. 主要结论与发现
*   **性能卓越**：在相同剪枝比例下，HFPrune 始终优于现有方法。在 Llama2-7B 剪掉 20% 参数后，其性能甚至超过了原始密集模型。
*   **高保真度**：信息熵准则能更有效地保持原始模型的输出分布形状（JS 散度更低），从而保留了更多“暗知识”。
*   **策略优选**：实验证明，相比于粗粒度的注意力头剪枝，针对 MLP 模块的细粒度神经元剪枝在性能恢复上更具潜力。

### 7. 优点与亮点
*   **标签无关（Label-free）**：无需标注数据即可进行重要性评估，极大扩展了应用场景。
*   **高效简洁**：无需额外的教师模型，解决了自蒸馏剪枝计算开销大和初始梯度消失的问题。
*   **全局视角**：通过信息熵捕捉全概率分布的信息，比仅看 Top-1 概率的交叉熵更科学。

### 8. 不足与局限
*   **模块局限性**：虽然论文论证了 MLP 剪枝的优越性，但未深入探讨如何将该准则应用于注意力机制的复杂结构优化。
*   **评估维度**：主要集中在零样本任务，未广泛测试模型在长文本生成、多轮对话或特定领域（如代码、数学）的性能变化。
*   **微调依赖**：虽然剪枝准则优秀，但最终性能仍高度依赖于后期的指令微调阶段。

（完）
