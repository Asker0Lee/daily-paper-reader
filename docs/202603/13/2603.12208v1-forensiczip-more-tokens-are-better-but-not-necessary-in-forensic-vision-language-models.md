---
title: "ForensicZip: More Tokens are Better but Not Necessary in Forensic Vision-Language Models"
title_zh: ForensicZip：取证视觉语言模型中 Token 越多越好但并非必要
authors: "Yingxin Lai, Zitong Yu, Jun Wang, Linlin Shen, Yong Xu, Xiaochun Cao"
date: 2026-03-12
pdf: "https://arxiv.org/pdf/2603.12208v1"
tags: ["query:llm-cp"]
score: 9.0
evidence: 多模态大语言模型中用于高效推理的视觉标记剪枝
tldr: "针对多模态大模型在多媒体取证中计算开销大的问题，本文提出ForensicZip框架。该方法摒弃了传统的语义驱动剪枝，转而从伪造驱动视角出发，利用出生-死亡最优传输模型量化时间维度的物理不连续性，并结合高频先验提取关键取证证据。在仅保留10% Token的情况下，该框架实现了显著的推理加速和计算量降低，同时在多个基准测试上保持了最先进的检测性能。"
selection_source: fresh_fetch
motivation: 现有的视觉Token剪枝方法主要关注语义显著性，容易误删背景区域中包含的高频异常和时间抖动等关键伪造痕迹。
method: 引入一种无需训练的框架，通过建模Token演化的最优传输问题并结合高频先验，实现大比例压缩下的取证证据提取。
result: "在Deepfake和AIGC基准上，该方法在10% Token保留率下实现了2.97倍加速和90%以上的FLOPs降低。"
conclusion: 研究表明，在取证视觉语言模型中，通过伪造驱动的Token压缩可以在极低计算成本下维持甚至提升检测精度。
---

## 摘要
多模态大语言模型 (MLLMs) 通过为伪造检测生成文本解释，实现了可解释的多媒体取证。然而，处理密集的视觉序列会产生高昂的计算成本，尤其是在处理高分辨率图像和视频时。视觉 Token 剪枝是一种实用的加速策略，但现有方法大多是语义驱动的，即保留显著对象而丢弃背景区域，而高频异常和时间抖动等篡改痕迹往往存在于背景区域中。为了解决这一问题，我们引入了 ForensicZip，这是一个无需训练的框架，它从伪造驱动的角度重新定义了 Token 压缩。ForensicZip 将时间维度的 Token 演化建模为带有松弛虚拟节点的生灭最优传输 (Birth-Death Optimal Transport) 问题，从而量化指示瞬态生成伪影的物理不连续性。取证评分进一步结合了基于传输的新颖性与高频先验，以便在大比例压缩下将取证证据与语义内容分离。在 Deepfake 和 AIGC 基准测试上的实验表明，在 10% 的 Token 保留率下，ForensicZip 实现了 2.97 倍的加速和超过 90% 的 FLOPs 减少，同时保持了最先进的检测性能。

## Abstract
Multimodal Large Language Models (MLLMs) enable interpretable multimedia forensics by generating textual rationales for forgery detection. However, processing dense visual sequences incurs high computational costs, particularly for high-resolution images and videos. Visual token pruning is a practical acceleration strategy, yet existing methods are largely semantic-driven, retaining salient objects while discarding background regions where manipulation traces such as high-frequency anomalies and temporal jitters often reside. To address this issue, we introduce ForensicZip, a training-free framework that reformulates token compression from a forgery-driven perspective. ForensicZip models temporal token evolution as a Birth-Death Optimal Transport problem with a slack dummy node, quantifying physical discontinuities indicating transient generative artifacts. The forensic scoring further integrates transport-based novelty with high-frequency priors to separate forensic evidence from semantic content under large-ratio compression. Experiments on deepfake and AIGC benchmarks show that at 10\% token retention, ForensicZip achieves $2.97\times$ speedup and over 90\% FLOPs reduction while maintaining state-of-the-art detection performance.

---

## 论文详细总结（自动生成）

### ForensicZip：取证视觉语言模型中 Token 越多越好但并非必要 —— 论文深度总结

#### 1. 核心问题与整体含义（研究动机和背景）
*   **研究动机**：多模态大语言模型（MLLM）在多媒体取证（如 Deepfake 检测）中表现出色，能提供可解释的伪造依据。然而，处理高分辨率图像或长视频会产生海量的视觉 Token，导致预填充（Prefill）阶段计算开销巨大。
*   **核心问题**：现有的视觉 Token 剪枝方法大多是**语义驱动**的（保留显著物体，丢弃背景）。但在取证任务中，伪造痕迹（如高频噪声、边缘融合痕迹、时间抖动）往往存在于语义不显著的背景区域。简单套用现有剪枝方法会导致关键取证证据丢失，在大幅压缩时性能骤降。

#### 2. 方法论：核心思想与关键技术
ForensicZip 是一个**无需训练（Training-free）**的框架，将 Token 压缩从“语义驱动”转向“**伪造驱动**”。
*   **传输新颖性估计 (Transport Novelty Estimation, TNE)**：
    *   **核心思想**：利用物理连续性原理。自然视频帧间演化平滑，而生成式伪影会打破这种连续性。
    *   **技术细节**：将相邻帧间的 Token 演化建模为**带有虚拟节点（Dummy Node）的生灭最优传输（Birth-Death Optimal Transport）问题**。
    *   **算法流程**：通过 Sinkhorn 迭代求解传输方案。如果某个 Token 无法在上一帧找到匹配来源，其质量会流向虚拟节点，从而产生高额的“出生（Birth）”或“死亡（Death）”成本，这些高成本区域即为潜在的伪造痕迹。
*   **取证评分与选择 (Forensic Scoring, FS)**：
    *   **空间调制**：引入高频先验（使用 Laplacian 算子），因为生成伪影常伴随异常的高频响应。
    *   **综合评分**：将时间维度的传输成本与空间维度的高频得分相乘（类似于“逻辑与”门），只有在时空两域都异常的 Token 才会获得高分。
*   **物理剪枝**：根据评分进行 Top-K 选择，直接减少进入 LLM 的序列长度，实现端到端加速。

#### 3. 实验设计
*   **数据集/场景**：
    *   **通用与 AIGC 检测**：FakeVLM (FakeClue, LOKI), DMimage, FakeShield-AIGC, SIDA。
    *   **Deepfake 取证**：DD-VQA, FakeShield-DeepFake, DFFD。
    *   **传统篡改与幻觉测试**：FakeShield-PhotoShop, POPE。
*   **Benchmark 与对比方法**：
    *   **基准模型**：LLaVA-OneVision, SIDA, FakeVLM, FakeShield 等。
    *   **对比剪枝方法**：FastV, SparseVLM, LLaVA-Scissor, VisionTrim。
*   **评价指标**：准确率 (Acc)、F1 分数、FLOPs 减少量、推理延迟 (Latency)、GPU 显存占用、吞吐量。

#### 4. 资源与算力
*   **硬件环境**：实验主要在 **NVIDIA A100 (80GB)** GPU 上进行。
*   **训练时长**：由于该方法是**无需训练（Training-free）**的插件式框架，因此不存在模型训练时长。
*   **推理开销**：最优传输（OT）求解器的计算开销相对于 Transformer 层的节省几乎可以忽略不计（线性扩展）。

#### 5. 实验数量与充分性
*   **实验规模**：涵盖了从图像到视频、从 AI 生成到传统 PS 篡改的 10 余个主流取证数据集。
*   **消融实验**：详细验证了 TNE 模块、FS 模块、不同空间算子（Sobel vs Laplacian）、以及超参数（正则化强度、迭代次数、惩罚项）的影响。
*   **客观性**：对比了 2024-2026 年间最新的 SOTA 剪枝方法，并在不同 Token 保留率（50%, 25%, 10%）下进行了全方位对比，实验设计严谨且公平。

#### 6. 主要结论与发现
*   **性能领先**：在仅保留 **10% Token** 的极端情况下，ForensicZip 在 FakeClue 上的准确率仍高达 97.74%，而传统语义剪枝方法性能会崩溃（降至 60% 左右）。
*   **高效加速**：实现了 **2.97 倍** 的推理加速，并减少了超过 **90%** 的 FLOPs。
*   **显存优化**：在 LLaVA-OV-7B 模型上，峰值显存从 26.7GB 降至 18.5GB。
*   **意外收获**：在 POPE 目标幻觉测试中，该方法甚至略微提升了原始模型的准确率，说明去除冗余语义噪声有助于模型更精准地聚焦证据。

#### 7. 优点（亮点）
*   **范式创新**：首次指出语义显著性与取证证据之间的“空间正交性”，提出了伪造驱动的剪枝逻辑。
*   **数学严谨**：引入最优传输理论中的“生灭”机制，能够优雅地量化生成式伪影带来的瞬态不连续性。
*   **即插即用**：无需任何微调或重新训练，可直接适配现有的各种取证 MLLM。

#### 8. 不足与局限
*   **单图限制**：虽然支持单图（退化为空间新颖性计算），但其核心优势在于视频序列的时间动态分析，单图模式下的增益相对较小。
*   **超参数依赖**：虽然实验证明其对超参数较鲁棒，但在极端不同的视频编码或分辨率下，可能仍需微调生灭惩罚项 $c$。
*   **极端攻击**：对于专门针对物理一致性进行优化的极高水平伪造（如完美的时间平滑生成），该方法的检测效力可能会受到挑战。

（完）
