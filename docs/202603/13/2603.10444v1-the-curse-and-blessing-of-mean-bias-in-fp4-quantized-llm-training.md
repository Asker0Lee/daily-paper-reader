---
title: The Curse and Blessing of Mean Bias in FP4-Quantized LLM Training
title_zh: FP4 量化大语言模型训练中均值偏差的诅咒与祝福
authors: "Hengjie Cao, Zhendong Huang, Mengyi Chen, Yifeng Yang, Fanqi Yu, Ruijun Huang, Fang Dong, Xin Zhang, Jixian Zhou, Anrui Chen, Mingzhi Dong, Yujiang Wang, Jinlong Hou, Qin Lv, Yuan Cheng, Tun Lu, Fan Yang, Li Shang"
date: 2026-03-11
pdf: "https://arxiv.org/pdf/2603.10444v1"
tags: ["query:llm-cp"]
score: 9.0
evidence: FP4量化LLM训练及数值不稳定性分析
tldr: 本研究探讨了FP4量化LLM训练中的不稳定性问题。研究发现，LLM表示中的各向异性（尤其是秩为1的均值偏差）是导致低比特训练不稳定的主因，因为它拉大了动态范围并压缩了语义细节。作者提出了一种简单的源级均值减法操作来消除此偏差。实验证明，该方法在FP4（W4A4G4）训练中显著缩小了与BF16的性能差距，为稳定、高效的低比特LLM训练提供了新路径。
selection_source: fresh_fetch
motivation: LLM表示中的各向异性导致极端激活值出现，从而在低比特量化训练中引发数值不稳定。
method: 通过在源头执行简单的均值减法操作，消除导致动态范围膨胀的秩为1的均值偏差。
result: 在FP4（W4A4G4）训练实验中，该方法显著降低了训练损失并恢复了下游任务性能，接近BF16水平。
conclusion: 均值偏差消除是一种硬件友好且高效的策略，能有效解决低精度LLM训练中的不稳定性挑战。
---

## 摘要
在自然语言上训练的大语言模型表现出显著的各向异性：少数方向集中了不成比例的能量，而其余维度则形成了广泛的语义尾部。在低比特训练方案中，这种几何结构变得数值不稳定。由于分块量化比例是由极端的逐元素量级决定的，主导方向拉伸了动态范围，将长尾语义变化压缩到狭窄的数值区间中。我们表明，这种不稳定性主要由一种相干的秩一均值偏差驱动，该偏差构成了大语言模型表示中频谱各向异性的主导成分。这种均值成分在不同层和训练阶段系统性地出现，并占据了极端激活量级的大部分，使其成为低精度下动态范围膨胀的主要驱动因素。至关重要的是，由于主导不稳定性是秩一的，可以通过简单的源级均值减法操作来消除。这种以偏差为中心的调节恢复了基于 SVD 的频谱方法的大部分稳定性优势，同时仅需要归约操作和标准量化算子。在 FP4 (W4A4G4) 训练上的实验结果表明，均值移除显著缩小了与 BF16 的损失差距并恢复了下游性能，为稳定的低比特大语言模型训练提供了一条硬件高效的路径。

## Abstract
Large language models trained on natural language exhibit pronounced anisotropy: a small number of directions concentrate disproportionate energy, while the remaining dimensions form a broad semantic tail. In low-bit training regimes, this geometry becomes numerically unstable. Because blockwise quantization scales are determined by extreme elementwise magnitudes, dominant directions stretch the dynamic range, compressing long-tail semantic variation into narrow numerical bins. We show that this instability is primarily driven by a coherent rank-one mean bias, which constitutes the dominant component of spectral anisotropy in LLM representations. This mean component emerges systematically across layers and training stages and accounts for the majority of extreme activation magnitudes, making it the principal driver of dynamic-range inflation under low precision. Crucially, because the dominant instability is rank-one, it can be eliminated through a simple source-level mean-subtraction operation. This bias-centric conditioning recovers most of the stability benefits of SVD-based spectral methods while requiring only reduction operations and standard quantization kernels. Empirical results on FP4 (W4A4G4) training show that mean removal substantially narrows the loss gap to BF16 and restores downstream performance, providing a hardware-efficient path to stable low-bit LLM training.

---

## 论文详细总结（自动生成）

这篇论文深入探讨了在大语言模型（LLM）进行 FP4（4位浮点数）量化训练时，激活值表现出的“各向异性”及其对数值稳定性的影响，并提出了一种高效的解决方案。以下是对该论文的结构化总结：

### 1. 核心问题与整体含义
*   **研究背景**：LLM 在训练过程中，其表示空间表现出显著的**各向异性（Anisotropy）**，即少数维度集中了极高的能量，形成“离群值（Outliers）”。
*   **核心问题**：在低比特（如 FP4）训练中，分块量化（Blockwise Quantization）的缩放因子由块内的最大绝对值决定。这些极端的离群值会拉伸动态范围，导致大部分正常的语义信息被压缩到极窄的数值区间，从而引发训练不稳定和精度大幅下降。
*   **研究动机**：作者发现这种不稳定性主要由一个简单的**秩一均值偏差（Rank-one Mean Bias）**驱动。如果能有效处理这个均值成分，就能在不使用复杂算法的情况下稳定低比特训练。

### 2. 方法论：Averis (Averaging-Induced Residual Splitting)
*   **核心思想**：将激活矩阵分解为“相干均值成分”和“零均值残差成分”，并对两者进行独立量化。
*   **关键技术细节**：
    *   **均值-残差分解**：对于激活矩阵 $X$，计算其列均值向量 $\mu_X = \frac{1}{l} \mathbf{1}^\top X$。将 $X$ 拆分为均值部分 $M_X = \mathbf{1}\mu_X^\top$ 和残差部分 $X_R = X - M_X$。
    *   **独立量化**：分别对均值向量 $\bar{\mu}_X = \text{Quant}(\mu_X)$ 和残差矩阵 $\bar{X}_R = \text{Quant}(X_R)$ 进行 FP4 量化。
    *   **前向计算**：利用线性性质，将矩阵乘法拆分为 $\hat{Y} = \mathbf{1}(\bar{\mu}_X \bar{W}) + \bar{X}_R \bar{W}$。其中第一项是广播加法，计算开销极小。
    *   **反向传播**：对输出梯度 $D$ 同样进行均值-残差拆分和独立量化，确保梯度计算的稳定性。
*   **算法流程**：该方法仅涉及求和（Reduction）和逐元素减法（Elementwise），无需复杂的奇异值分解（SVD）或迭代正交化。

### 3. 实验设计
*   **模型与数据**：使用 **Qwen3-0.6B** 模型，在 **DCLM 数据集**上进行了 **100B tokens** 的大规模预训练。
*   **量化配置**：采用 **W4A4G4**（权重、激活、梯度均为 FP4）配置，使用 E2M1 格式及随机舍入（Stochastic Rounding）。
*   **Benchmark**：在 7 个下游任务上进行评估，包括 ARC-C、ARC-E、BoolQ、HellaSwag、LAMBADA、PIQA 和 RACE。
*   **对比方法**：
    1.  **BF16**：全精度基准。
    2.  **Vanilla FP4**：直接进行 FP4 量化，不进行任何结构化处理。
    3.  **Averis FP4**：本文提出的均值移除方法。

### 4. 资源与算力
*   **算力说明**：论文中**未明确说明**具体的 GPU 型号、数量或总训练时长。但考虑到 0.6B 模型训练 100B tokens 的规模，这属于中等规模的预训练实验。

### 5. 实验数量与充分性
*   **实验组数**：论文展示了完整的训练 Loss 曲线，并在 10B token 节点进行了详细的下游任务对比。
*   **分析深度**：作者进行了丰富的诊断性实验，包括：
    *   不同层、不同训练阶段的均值能量占比分析。
    *   算子级（Attention Softmax 和 FFN SwiGLU）对均值偏差的放大效应分析。
    *   离群值来源的消融分析（均值 vs. 频谱尖峰 vs. 残差）。
*   **充分性评价**：实验设计较为充分，不仅有宏观的性能对比，还有微观的数学证明（定理 1-3）来支撑其物理含义，逻辑链条完整。

### 6. 主要结论与发现
*   **均值偏差是主因**：LLM 激活值中的离群值主要由相干的均值偏差引起，而非随机噪声。
*   **自我强化机制**：均值偏差起源于嵌入层的频率分布，通过非线性激活函数（如 SwiGLU）再生，并通过残差连接不断累积。
*   **性能恢复**：Averis 方法显著缩小了 FP4 与 BF16 之间的 Loss 差距。在下游任务中，Averis 将平均得分从 Vanilla FP4 的水平提升至接近 BF16 的水平（0.4661 vs 0.4564）。
*   **“诅咒与祝福”**：均值偏差虽然是低比特训练的“诅咒”（导致不稳定），但由于其结构简单（秩一），也成为了“祝福”（极易被修复）。

### 7. 优点
*   **硬件友好**：相比于之前需要 SVD 分解的方法（如 Metis），Averis 仅需基础算子，极易在现有深度学习框架和硬件加速器上实现。
*   **理论扎实**：通过数学证明解释了为什么均值位移比方差波动更容易产生离群值。
*   **普适性**：揭示了 LLM 架构中残差网络和非线性层对各向异性的系统性贡献。

### 8. 不足与局限
*   **模型规模局限**：实验主要集中在 0.6B 参数规模，虽然训练 Token 数较多，但在 7B、70B 等更大规模模型上的表现尚未直接展示。
*   **精度残差**：尽管 Averis 效果显著，但 FP4 训练的 Loss 曲线仍略高于 BF16，说明低比特量化仍存在不可完全消除的截断误差。
*   **任务覆盖**：下游任务主要集中在常识推理，缺乏长文本、代码或数学等更复杂场景的评估。

（完）
