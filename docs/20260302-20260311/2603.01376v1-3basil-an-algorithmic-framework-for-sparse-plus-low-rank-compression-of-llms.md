---
title: "3BASiL: An Algorithmic Framework for Sparse plus Low-Rank Compression of LLMs"
title_zh: 3BASiL：一种用于大语言模型稀疏加低秩压缩的算法框架
authors: "Mehdi Makni, Xiang Meng, Rahul Mazumder"
date: 2026-03-02
pdf: "https://arxiv.org/pdf/2603.01376v1"
tags: ["query:llm-cp"]
score: 10.0
evidence: 大语言模型的稀疏加低秩分解压缩
tldr: 本研究针对大语言模型（LLM）在稀疏加低秩（S+LR）压缩中面临的性能退化问题，提出了3BASiL-TM框架。该框架首先利用基于三块ADMM的3BASiL算法最小化层级重建误差，随后通过Transformer匹配（TM）步骤跨层联合优化稀疏与低秩组件。实验证明，该方法在LLaMA-8B模型上显著缩小了困惑度差距，且压缩速度比现有SOTA方法快2.5倍以上，为高效模型压缩提供了新路径。
selection_source: fresh_fetch
motivation: 现有的稀疏加低秩分解方法在压缩大语言模型时，往往面临严重的性能损失和计算效率瓶颈。
method: 提出了一种结合三块ADMM层级优化与跨层Transformer匹配细化步骤的3BASiL-TM算法框架。
result: "在LLaMA-8B模型上，该方法将WikiText2困惑度差距降低了30%以上，且压缩速度提升了2.5倍。"
conclusion: 3BASiL-TM提供了一个高效且通用的LLM压缩方案，在保证模型精度的同时显著提升了压缩效率。
---

## 摘要
大语言模型（LLMs）的稀疏加低秩（$\mathbf{S} + \mathbf{LR}$）分解已成为模型压缩中一个极具前景的方向，旨在将预训练模型的权重分解为稀疏矩阵和低秩矩阵之和（$\mathbf{W} \approx \mathbf{S} + \mathbf{LR}$）。尽管最近取得了进展，但与稠密模型相比，现有方法往往面临严重的性能下降。在这项工作中，我们引入了 3BASiL-TM，这是一种用于 LLMs 的 $(\mathbf{S} + \mathbf{LR})$ 分解的高效单次（one-shot）训练后方法，填补了这一空白。我们的方法首先引入了一种新颖的三块交替方向乘子法（ADMM），称为 3BASiL，旨在最小化具有收敛保证的逐层重构误差。然后，我们设计了一个高效的 Transformer 匹配（TM）细化步骤，共同优化跨 Transformer 层的稀疏和低秩组件。该步骤最小化了一种新颖的内存高效损失，从而在 Transformer 级别对齐输出。值得注意的是，TM 过程具有通用性，因为它可以增强任何 $(\mathbf{S} + \mathbf{LR})$ 分解，包括纯稀疏化。我们的数值实验表明，在 (2:4 稀疏 + 64 低秩) 配置下，与之前的方法相比，3BASiL-TM 将相对于稠密 LLaMA-8B 模型的 WikiText2 困惑度差距缩小了 30% 以上。此外，与最先进的（SOTA）$(\mathbf{S} + \mathbf{LR})$ 方法相比，我们的方法在 A100 GPU 上的压缩运行速度提高了 2.5 倍以上。我们的代码可在 https://github.com/mazumder-lab/3BASiL 获取。

## Abstract
Sparse plus Low-Rank $(\mathbf{S} + \mathbf{LR})$ decomposition of Large Language Models (LLMs) has emerged as a promising direction in model compression, aiming to decompose pre-trained model weights into a sum of sparse and low-rank matrices $(\mathbf{W} \approx \mathbf{S} + \mathbf{LR})$. Despite recent progress, existing methods often suffer from substantial performance degradation compared to dense models. In this work, we introduce 3BASiL-TM, an efficient one-shot post-training method for $(\mathbf{S} + \mathbf{LR})$ decomposition of LLMs that addresses this gap. Our approach first introduces a novel 3-Block Alternating Direction Method of Multipliers (ADMM) method, termed 3BASiL, to minimize the layer-wise reconstruction error with convergence guarantees. We then design an efficient transformer-matching (TM) refinement step that jointly optimizes the sparse and low-rank components across transformer layers. This step minimizes a novel memory-efficient loss that aligns outputs at the transformer level. Notably, the TM procedure is universal as it can enhance any $(\mathbf{S} + \mathbf{LR})$ decomposition, including pure sparsity. Our numerical experiments show that 3BASiL-TM reduces the WikiText2 perplexity gap relative to dense LLaMA-8B model by over 30% under a (2:4 Sparse + 64 LR) configuration, compared to prior methods. Moreover, our method achieves over 2.5x faster compression runtime on an A100 GPU compared to SOTA $(\mathbf{S} + \mathbf{LR})$ method. Our code is available at https://github.com/mazumder-lab/3BASiL.

---

## 论文详细总结（自动生成）

这是一份关于论文《3BASiL: An Algorithmic Framework for Sparse plus Low-Rank Compression of LLMs》的结构化总结：

### 1. 核心问题与整体含义
*   **研究背景**：大语言模型（LLM）规模巨大，部署成本高。模型压缩技术（如剪枝和量化）是关键。
*   **核心问题**：最近的研究聚焦于将权重矩阵 $W$ 分解为“稀疏（Sparse）+ 低秩（Low-Rank）”的形式（即 $W \approx S + LR$）。这种结构不仅能压缩模型，还能作为 LoRA 微调的“智能初始化”。
*   **研究动机**：现有的 S+LR 分解方法（如 OATS, HASSLE-free）多采用简单的交替最小化算法，缺乏收敛性保证，且在联合优化稀疏与低秩组件时效率低下，导致压缩后模型性能下降严重。

### 2. 方法论
论文提出了 **3BASiL-TM** 框架，包含两个核心阶段：
*   **3BASiL (3-Block ADMM)**：
    *   **核心思想**：将层级重构误差最小化问题转化为一个受约束的优化问题，引入辅助变量 $D$ 作为稀疏组件 $S$ 的副本。
    *   **算法流程**：采用三块交替方向乘子法（ADMM），依次更新三个变量块：
        1.  **S-block**：通过求解一个正则化的最小二乘问题（利用预计算的特征值分解加速）获得稀疏权重的闭式解。
        2.  **L-block**：利用随机奇异值分解（Randomized SVD）在投影空间内寻找最佳低秩近似。
        3.  **D-block**：将 $S$ 投影到稀疏约束集（如 N:M 结构化剪枝或非结构化剪枝）。
    *   **理论贡献**：证明了在特定惩罚参数更新策略下，该算法具有收敛性保证。
*   **TM (Transformer-matching)**：
    *   **核心思想**：层级优化（Layer-wise）是局部最优，会产生误差累积。TM 步骤在 Transformer 块级别进行细化。
    *   **技术细节**：固定稀疏模式（Mask），使用 Adam 优化器在少量校准数据上联合微调 $S$ 和 $LR$ 的权重，使压缩后的 Transformer 块输出对齐原始稠密模型的输出。

### 3. 实验设计
*   **数据集**：
    *   **校准集**：从 C4 数据集中随机抽取 128 个样本。
    *   **评估集**：WikiText2, Penn Treebank (PTB), C4 验证集（用于困惑度 PPL 测试）。
    *   **任务**：8 个零样本（Zero-shot）推理任务（如 PIQA, ARC, HellaSwag 等）。
*   **Benchmark 模型**：Llama-3 (8B), Llama-3.2 (1B, 3B), OPT-30B。
*   **对比方法**：
    *   **S+LR 基线**：OATS, HASSLE-free-SparseGPT, HASSLE-free-ALPS, EoRA。
    *   **纯剪枝基线**：Wanda, SparseGPT, ALPS。

### 4. 资源与算力
*   **硬件使用**：NVIDIA A100 (80GB), L40 (48GB), H100 (80GB)。
*   **运行效率**：
    *   在 Llama3-8B (2:4 稀疏 + 64 秩) 配置下，3BASiL-TM 在 A100 上比 Hf-ALPS 快 **2.5 倍以上**。
    *   在 Llama3.2-3B 模型上，相比强基线实现了 **7 倍以上** 的加速。
    *   LoRA 微调阶段（1500 万 token）在 H100 上仅需约 0.7 小时。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了从 1B 到 30B 参数的不同架构模型，实验组数较多。
*   **配置覆盖**：测试了多种 N:M 稀疏模式（2:4, 3:8, 4:8）以及非结构化剪枝，并配合不同的低秩（Rank 64/128）。
*   **消融与对比**：包含了 TM 步骤对不同剪枝算法的通用性提升实验、LoRA 微调前后的性能对比、以及层级重构误差的直接对比。
*   **评价**：实验设计客观、公平，所有方法均使用相同的校准数据和评估协议，对比维度全面。

### 6. 主要结论与发现
*   **性能领先**：3BASiL-TM 显著优于现有 S+LR 方法。在 Llama-8B 上，它将 WikiText2 的困惑度差距缩小了 **30% 以上**。
*   **TM 的通用性**：TM 步骤不仅适用于 3BASiL，还能显著提升 Wanda 或 SparseGPT 等纯剪枝方法的性能。
*   **初始化优势**：S+LR 分解产生的低秩部分作为 LoRA 的初始化，比随机初始化或单纯的剪枝后加适配器效果更好。
*   **收敛性与误差**：3BASiL 算法在层级重构误差上达到了比交替最小化算法更低的值。

### 7. 优点
*   **算法严谨性**：首次为 LLM 的 S+LR 分解引入了具有收敛性保证的 3-Block ADMM 框架。
*   **高效性**：通过数学技巧（如特征值分解预计算）避免了昂贵的矩阵求逆，大幅提升了单次压缩的速度。
*   **内存友好**：TM 步骤设计精巧，仅需在 Transformer 块级别进行前向/后向传播，内存开销可控。

### 8. 不足与局限
*   **超参数依赖**：ADMM 算法的性能高度依赖于惩罚参数 $\rho$ 的更新策略，虽然文中给出了启发式规则，但在不同模型上可能需要微调。
*   **硬件加速限制**：虽然 N:M 稀疏在理论上支持加速，但实际推理速度提升仍依赖于底层 CUDA 内核的优化程度。
*   **未结合量化**：该框架目前专注于“稀疏+低秩”，尚未讨论如何将其与 4-bit 或 8-bit 量化进一步结合，这在实际部署中也是重要的一环。

（完）
