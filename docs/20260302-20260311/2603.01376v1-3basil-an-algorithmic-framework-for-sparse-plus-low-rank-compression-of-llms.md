---
title: "3BASiL: An Algorithmic Framework for Sparse plus Low-Rank Compression of LLMs"
title_zh: 3BASiL：一种用于大语言模型稀疏加低秩压缩的算法框架
authors: "Mehdi Makni, Xiang Meng, Rahul Mazumder"
date: 2026-03-02
pdf: "https://arxiv.org/pdf/2603.01376v1"
tags: ["query:llm-cp"]
score: 10.0
evidence: 大模型压缩中的稀疏加低秩分解
tldr: 本研究针对大语言模型（LLM）的稀疏加低秩（S+LR）压缩方法中存在的性能下降问题，提出了3BASiL-TM框架。该框架包含一种基于三块交替方向乘子法（ADMM）的层级重构算法3BASiL，以及一种跨层联合优化的Transformer匹配（TM）细化步骤。实验表明，该方法在保持高效压缩的同时，显著缩小了与稠密模型的性能差距，且运行速度比现有最先进方法快2.5倍以上。
selection_source: fresh_fetch
motivation: 现有的稀疏加低秩分解方法在压缩大语言模型时往往会导致严重的性能退化。
method: 提出了一种结合三块ADMM层级重构与跨层Transformer匹配细化步骤的单次训练后压缩框架。
result: "在LLaMA-8B模型上，该方法将困惑度差距缩小了30%以上，且压缩速度提升了2.5倍。"
conclusion: 3BASiL-TM为大语言模型提供了一种高效且高性能的稀疏加低秩压缩方案，具有较强的通用性。
---

## 摘要
大语言模型（LLMs）的稀疏加低秩（$\mathbf{S} + \mathbf{LR}$）分解已成为模型压缩中一个极具前景的方向，旨在将预训练模型的权重分解为稀疏矩阵和低秩矩阵之和（$\mathbf{W} \approx \mathbf{S} + \mathbf{LR}$）。尽管最近取得了进展，但与稠密模型相比，现有方法往往面临严重的性能下降。在这项工作中，我们引入了 3BASiL-TM，这是一种用于 LLMs 的 $(\mathbf{S} + \mathbf{LR})$ 分解的高效单次（one-shot）训练后方法，填补了这一空白。我们的方法首先引入了一种新颖的三块交替方向乘子法（ADMM），称为 3BASiL，旨在最小化具有收敛保证的逐层重构误差。然后，我们设计了一个高效的 Transformer 匹配（TM）细化步骤，共同优化跨 Transformer 层的稀疏和低秩组件。该步骤最小化了一种新颖的内存高效损失，从而在 Transformer 级别对齐输出。值得注意的是，TM 过程具有通用性，因为它可以增强任何 $(\mathbf{S} + \mathbf{LR})$ 分解，包括纯稀疏化。我们的数值实验表明，在 (2:4 稀疏 + 64 低秩) 配置下，与之前的方法相比，3BASiL-TM 将相对于稠密 LLaMA-8B 模型的 WikiText2 困惑度差距缩小了 30% 以上。此外，与最先进的（SOTA）$(\mathbf{S} + \mathbf{LR})$ 方法相比，我们的方法在 A100 GPU 上的压缩运行速度提高了 2.5 倍以上。我们的代码可在 https://github.com/mazumder-lab/3BASiL 获取。

## Abstract
Sparse plus Low-Rank $(\mathbf{S} + \mathbf{LR})$ decomposition of Large Language Models (LLMs) has emerged as a promising direction in model compression, aiming to decompose pre-trained model weights into a sum of sparse and low-rank matrices $(\mathbf{W} \approx \mathbf{S} + \mathbf{LR})$. Despite recent progress, existing methods often suffer from substantial performance degradation compared to dense models. In this work, we introduce 3BASiL-TM, an efficient one-shot post-training method for $(\mathbf{S} + \mathbf{LR})$ decomposition of LLMs that addresses this gap. Our approach first introduces a novel 3-Block Alternating Direction Method of Multipliers (ADMM) method, termed 3BASiL, to minimize the layer-wise reconstruction error with convergence guarantees. We then design an efficient transformer-matching (TM) refinement step that jointly optimizes the sparse and low-rank components across transformer layers. This step minimizes a novel memory-efficient loss that aligns outputs at the transformer level. Notably, the TM procedure is universal as it can enhance any $(\mathbf{S} + \mathbf{LR})$ decomposition, including pure sparsity. Our numerical experiments show that 3BASiL-TM reduces the WikiText2 perplexity gap relative to dense LLaMA-8B model by over 30% under a (2:4 Sparse + 64 LR) configuration, compared to prior methods. Moreover, our method achieves over 2.5x faster compression runtime on an A100 GPU compared to SOTA $(\mathbf{S} + \mathbf{LR})$ method. Our code is available at https://github.com/mazumder-lab/3BASiL.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **3BASiL-TM** 的算法框架，旨在通过“稀疏加低秩”（Sparse plus Low-Rank, $S+LR$）分解技术来压缩大语言模型（LLM）。以下是对该论文的深度结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：LLM 巨大的参数量导致部署成本极高。现有的压缩技术（如剪枝、量化）在极高压缩率下会导致性能显著下降。
*   **研究背景**：$S+LR$ 分解（$W \approx S + LR$）是一种新兴方向，它将权重分解为稀疏矩阵（捕捉离群值/重要权重）和低秩矩阵（捕捉整体结构）。
*   **动机**：现有的 $S+LR$ 方法（如 OATS, HASSLE-free）多采用交替最小化算法，缺乏收敛性保证，且在联合优化稀疏与低秩组件时效率较低，导致压缩后的模型与原始稠密模型之间存在较大的性能差距。

### 2. 论文提出的方法论
该框架由两个核心部分组成：**3BASiL** 算法和 **TM** 细化步骤。
*   **3BASiL (3-Block ADMM for Sparsity and Low-Rank)**：
    *   **核心思想**：将层级重构误差最小化问题转化为一个受约束的优化问题，并引入辅助变量，利用**三块交替方向乘子法（3-Block ADMM）**进行求解。
    *   **算法流程**：将变量分为稀疏组件 $S$、低秩组件 $L$ 和稀疏约束副本 $D$。
        *   **S-block 更新**：通过闭式解更新 $S$，考虑了输入激活值的协方差。
        *   **L-block 更新**：利用奇异值分解（SVD）或随机 SVD 寻找最佳低秩近似。
        *   **D-block 更新**：通过投影算子强制执行稀疏性约束（如 N:M 剪枝或非结构化剪枝）。
    *   **理论保证**：论文证明了在惩罚参数满足特定增长条件下，该算法具有收敛性。
*   **TM (Transformer Matching) 细化**：
    *   **核心思想**：在完成逐层压缩后，通过匹配整个 Transformer 块（Block）的输出来进一步微调 $S$ 和 $L$。
    *   **技术细节**：这是一种内存高效的优化步骤，使用一小部分校准数据，通过梯度下降联合优化块内所有层的稀疏支持集权重和低秩矩阵，从而减少层级误差的累积。

### 3. 实验设计
*   **数据集**：
    *   **校准数据**：从 C4 数据集中随机抽取 128 个样本。
    *   **评估指标**：WikiText2、Penn Treebank (PTB) 和 C4 的困惑度（Perplexity）；以及 PIQA、ARC、HellaSwag 等 8 个零样本（Zero-shot）任务的准确率。
*   **Benchmark 与对比方法**：
    *   **模型**：Llama-3 (8B)、Llama-3.2 (1B, 3B) 和 OPT-30B。
    *   **对比方法**：OATS、HASSLE-free-SparseGPT、HASSLE-free-ALPS、SparseGPT、ALPS、Wanda 等。
    *   **配置**：重点测试了 N:M 结构化稀疏（如 2:4, 3:8, 4:8）结合不同秩（如 64, 112）的低秩适配器。

### 4. 资源与算力
*   **硬件设备**：使用了 NVIDIA **A100 (80GB)**、**L40 (48GB)** 和 **H100 (80GB)** GPU。
*   **运行效率**：
    *   3BASiL 相比 HASSLE-free-ALPS 基准实现了 **7 倍以上**的加速（在 L40 上压缩 Llama-3.2-3B）。
    *   在 A100 上，3BASiL-TM 的整体压缩速度比 SOTA 方法快 **2.5 倍以上**。
    *   例如，Llama-3-8B 的 (2:4+64LR) 配置压缩耗时显著低于前人方法（具体见文中图 2）。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了从 1B 到 30B 参数规模的模型，测试了多种稀疏模式（结构化与非结构化）和多种压缩率。
*   **充分性**：进行了消融实验（如 TM 步骤的增益分析）、下游 LoRA 微调实验以及与不同剪枝算法（如 OWL 稀疏分配）的集成实验。
*   **客观性**：所有方法均在相同的校准数据和评估协议下运行，对比具有公平性。

### 6. 主要结论与发现
*   **性能提升**：在 (2:4 稀疏 + 64 秩) 配置下，3BASiL-TM 将 Llama-3-8B 的 WikiText2 困惑度差距缩小了 **30% 以上**。
*   **TM 的通用性**：TM 步骤不仅适用于 $S+LR$，还能显著提升纯剪枝方法（如 Wanda, SparseGPT）的性能。
*   **收敛性与质量**：3BASiL 能够比交替最小化方法更有效地降低层级重构误差（Objective Value 更低）。
*   **LoRA 友好**：分解得到的低秩部分为后续的 LoRA 微调提供了极佳的“智能初始化”，恢复性能的速度更快。

### 7. 优点
*   **算法创新**：首次将 3-Block ADMM 引入 LLM 的 $S+LR$ 分解，并提供了收敛性证明。
*   **高效性**：通过闭式解和随机 SVD 优化，大幅降低了计算开销。
*   **跨层优化**：TM 步骤弥补了逐层压缩忽略层间交互的缺陷，且保持了较低的内存占用。
*   **实用性强**：支持 N:M 稀疏，能够直接利用现代 GPU 的硬件加速特性。

### 8. 不足与局限
*   **模型覆盖**：实验主要集中在 Llama 和 OPT 系列，尚未在其他架构（如混合专家模型 MoE）上验证。
*   **超参数敏感性**：ADMM 的惩罚参数 $\rho$ 的更新策略对结果有一定影响，可能需要针对不同模型微调。
*   **自动化程度**：虽然集成了 OWL，但如何自动为不同层分配最优的稀疏度与秩（Rank）仍是一个待探索的课题。
*   **量化集成**：目前主要关注稀疏与低秩，尚未深入探讨与 4-bit 或更低位宽量化的深度融合。

（完）
