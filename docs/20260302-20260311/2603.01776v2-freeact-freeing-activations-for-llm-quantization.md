---
title: "FreeAct: Freeing Activations for LLM Quantization"
title_zh: FreeAct：释放大语言模型量化中的激活
authors: "Xiaohao Liu, Xiaobo Xia, Manyi Zhang, Ji-Fu Li, Xianzhi Yu, Fei Shen, Xiu Su, See-Kiong Ng, Tat-Seng Chua"
date: 2026-03-02
pdf: "https://arxiv.org/pdf/2603.01776v2"
tags: ["query:llm-cp"]
score: 10.0
evidence: LLM动态激活量化框架
tldr: "针对大语言模型量化中静态变换无法适应动态激活模式的问题，本文提出 FreeAct 框架。该方法利用激活值的秩亏特性，打破了传统的权重与激活值一对一变换的限制，允许为不同类型的 token（如视觉、文本或掩码 token）分配独立的激活变换矩阵，同时保持权重变换统一。在扩散模型和多模态模型上的实验表明，FreeAct 显著提升了量化性能，最高提升达 5.3%。"
selection_source: fresh_fetch
motivation: 现有的基于变换的量化方法采用静态的一对一约束，难以处理扩散模型和多模态模型中动态变化的激活分布。
method: 利用激活值的秩亏特性解耦权重与激活的变换，为不同类型的 token 分配特定的激活变换矩阵，同时维持权重的静态变换。
result: "在扩散 LLM 和多模态 LLM 上的实验显示，FreeAct 相比基准方法取得了显著的性能提升，最高可达 5.3%。"
conclusion: FreeAct 通过放宽静态变换约束，有效地解决了复杂模型中的激活差异问题，为高效 LLM 量化提供了新思路。
---

## 摘要
量化对于减轻大语言模型（LLMs）显著的内存和计算开销至关重要。虽然新兴的基于变换的方法通过使用正交矩阵将特征空间投影到更平滑的流形上，成功地增强了量化效果，但它们通常强制执行严格的一对一变换约束。这种静态方法未能考虑到输入激活中固有的动态模式，特别是在扩散大语言模型（dLLMs）和多模态大语言模型（MLLMs）中，其中不同的 token 类型表现出截然不同的分布。为了推进这一领域，我们提出了 FreeAct，这是一个新颖的量化框架，它放宽了静态的一对一约束，以适应动态的激活差异。在理论上，我们利用激活的秩亏（rank-deficient）特性推导出一个超出简单逆矩阵的解空间，从而实现了激活变换与权重的解耦。在方法论上，FreeAct 识别特定 token 的动态（即视觉与文本，或掩码 token），并为激活侧分配不同的变换矩阵，同时保持权重的统一静态变换。在 dLLMs 和 MLLMs 上的广泛实验表明，FreeAct 显著优于基准模型，性能提升高达 5.3%，并进行了深入分析。我们的代码将公开发布。

## 速览
**TLDR**：针对大语言模型量化中静态变换无法适应动态激活模式的问题，本文提出 FreeAct 框架。该方法利用激活值的秩亏特性，打破了传统的权重与激活值一对一变换的限制，允许为不同类型的 token（如视觉、文本或掩码 token）分配独立的激活变换矩阵，同时保持权重变换统一。在扩散模型和多模态模型上的实验表明，FreeAct 显著提升了量化性能，最高提升达 5.3%。 \
**Motivation**：现有的基于变换的量化方法采用静态的一对一约束，难以处理扩散模型和多模态模型中动态变化的激活分布。 \
**Method**：利用激活值的秩亏特性解耦权重与激活的变换，为不同类型的 token 分配特定的激活变换矩阵，同时维持权重的静态变换。 \
**Result**：在扩散 LLM 和多模态 LLM 上的实验显示，FreeAct 相比基准方法取得了显著的性能提升，最高可达 5.3%。 \
**Conclusion**：FreeAct 通过放宽静态变换约束，有效地解决了复杂模型中的激活差异问题，为高效 LLM 量化提供了新思路。

---

## Abstract
Quantization is pivotal for mitigating the significant memory and computational overhead of Large Language Models (LLMs). While emerging transformation-based methods have successfully enhanced quantization by projecting feature spaces onto smoother manifolds using orthogonal matrices, they typically enforce a rigid one-to-one transformation constraint. This static approach fails to account for the dynamic patterns inherent in input activations, particularly within diffusion LLMs (dLLMs) and Multimodal LLMs (MLLMs), where varying token types exhibit distinct distributions. To advance this, we propose FreeAct, a novel quantization framework that relaxes the static one-to-one constraint to accommodate dynamic activation disparities. Theoretically, we leverage the rank-deficient nature of activations to derive a solution space that extends beyond simple inverse matrices, enabling the decoupling of activation transformations from weights. Methodologically, FreeAct identifies token-specific dynamics (i.e., vision v.s. text, or masked tokens) and allocates distinct transformation matrices to the activation side, while maintaining a unified, static transformation for the weights. Extensive experiments across dLLMs and MLLMs demonstrate that FreeAct significantly outperforms baselines, up to 5.3% performance improvement, with in-depth analyses. Our code will be publicly released.

---

## 论文详细总结（自动生成）

以下是对论文《FreeAct: Freeing Activations for LLM Quantization》的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
量化是降低大语言模型（LLM）内存和计算开销的关键技术。目前最先进的量化方法（如 QuaRot, FlatQuant）通过正交变换平滑特征空间，但它们遵循严格的**“一对一”静态变换约束**：即激活侧和权重侧必须使用互为逆矩阵的单一变换，以保证计算等价性。
然而，在**扩散 LLM (dLLM)** 和**多模态 LLM (MLLM)** 中，输入激活表现出显著的**动态模式差异**（例如：掩码 token 与非掩码 token 之间、视觉 token 与文本 token 之间分布截然不同）。静态的一对一变换无法同时兼顾这些差异化的分布，导致在低比特（如 W4A4）量化下性能大幅下降。

### 2. 方法论：核心思想与关键技术
**FreeAct** 的核心思想是**放宽静态的一对一约束**，允许激活侧根据 token 类型使用不同的变换矩阵，而权重侧保持统一的静态变换。

*   **理论基础（超越逆矩阵）**：论文证明了由于 LLM 激活值通常具有**秩亏（Rank-deficient）**特性，其解空间并不局限于唯一的逆矩阵。通过利用激活矩阵的零空间（Kernel Space），可以设计出既能平滑激活分布又能保持计算等价性的非对称变换。
*   **Token 索引（Token Indexing）**：根据 token 类型（如 [MASK] 标识或模态标识）对输入序列进行分类索引。
*   **动态分配（Dynamic Allocation）**：
    *   构造一个包含“共享部分”和“唯一部分”的变换矩阵。
    *   **激活侧**：为不同类型的 token 分配特定的子空间基向量，并对不相关的部分进行**零填充（Zero-padding）**，以消除不同类型 token 间的信息纠缠。
    *   **权重侧**：使用一个统一的正交矩阵 $eP$，它整合了所有激活侧所需的子空间基。
*   **误差最小化**：通过在校准数据集上最小化量化前后的输出误差（MSE），利用 AdamW 优化器协同优化变换矩阵和可学习的裁剪阈值（Clip Threshold）。

### 3. 实验设计
*   **模型对象**：
    *   **dLLMs**：LLaDA-8B, Dream-7B。
    *   **MLLMs**：Qwen2.5-VL-7B, InternVL2.5-8B。
*   **数据集与基准**：
    *   **文本/数学任务**：HumanEval, GSM8K, Math500。
    *   **多模态任务**：MMMU, MMBench, RealworldQA。
    *   **校准集**：WikiText2（用于 dLLM），COCO-Cap（用于 MLLM）。
*   **对比方法**：RTN（四舍五入量化）、SmoothQuant、QuaRot、FlatQuant（当前的 SOTA 变换量化方法）。
*   **量化设置**：主要针对极具挑战性的 **W4A4**（权重 4-bit，激活 4-bit）设置。

### 4. 资源与算力
*   **训练细节**：使用了 AdamW 优化器，学习率 1e-3，训练 15 个 epoch，batch size 为 4。
*   **算力说明**：论文**未明确说明**具体的 GPU 型号、数量或总训练时长。但提到通过保存中间层隐藏状态实现了各层并行优化，这通常能显著缩短校准时间。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 4 个主流模型、6 个权威基准测试。
*   **消融实验**：
    *   验证了激活值的秩亏特性对性能的影响。
    *   对比了“可学习裁剪阈值”与“变换矩阵”各自的贡献。
    *   分析了全量应用 FreeAct 与部分层应用的差异。
*   **充分性评价**：实验设计较为充分，覆盖了当前 LLM 领域最前沿的扩散和多模态范式。对比实验包含了从基础量化到最新 SOTA 的多种方法，且提供了激活分布的可视化分析，论证过程客观、公平。

### 6. 主要结论与发现
*   **性能领先**：FreeAct 在所有测试场景下均优于现有方法，相比 FlatQuant 实现了高达 **5.3%** 的相对性能提升。
*   **动态性至关重要**：实验证实，为不同 token 类型分配独立的变换空间能有效缓解 W4A4 量化带来的精度损失，甚至在某些任务上接近 W8A8 或 BF16 的水平。
*   **秩亏特性的利用**：验证了 LLM 激活值确实存在冗余维度，这为非对称变换提供了理论上的可行性。

### 7. 优点
*   **范式创新**：首次打破了量化变换中“一对一”的思维定式，提出了“多对一”的灵活架构。
*   **理论严密**：提供了关于解空间扩展和投影不变性的数学证明。
*   **兼容性强**：该方法可以无缝集成到现有的变换量化框架（如 FlatQuant）中，且不增加推理时的权重存储负担。
*   **通用性**：统一了多模态和扩散模型这两种不同架构下的量化处理原则。

### 8. 不足与局限
*   **类型限制**：目前主要针对两种 token 类型（如视觉 vs 文本）进行解耦，对于更复杂的混合模态（如同时包含音频、视频、文本）的扩展性尚待验证。
*   **硬件部署**：论文主要侧重于算法模拟（Fake Quantization），未提供在特定硬件（如 NVIDIA Tensor Core）上的实际算子开发和端到端推理加速实测。
*   **超参数敏感性**：子空间秩（$r, r_1, r_2$）的分配比例目前主要依靠经验设置，缺乏自动化的搜索或自适应调整机制。

（完）
