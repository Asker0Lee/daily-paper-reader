---
title: "Sparse-BitNet: 1.58-bit LLMs are Naturally Friendly to Semi-Structured Sparsity"
title_zh: Sparse-BitNet：1.58比特大语言模型天然适配半结构化稀疏
authors: "Di Zhang, Xun Wu, Shaohan Huang, Yudong Wang, Hanyong Shao, Yingbo Hao, Zewen Chi, Li Dong, Ting Song, Yan Xia, Zhifang Sui, Furu Wei"
date: 2026-03-05
pdf: "https://arxiv.org/pdf/2603.05168v1"
tags: ["query:llm-cp"]
score: 10.0
evidence: "结合了1.58比特量化与动态N:M稀疏化"
tldr: "M稀疏性的结合，提出了Sparse-BitNet框架。研究发现1.58比特模型比全精度模型更契合稀疏化，能在保持性能的同时容忍更高的稀疏度。通过动态N:M稀疏化与稳定训练机制，该框架在训练和推理中实现了高达1.30倍的加速，为构建高效大语言模型提供了新方向。"
selection_source: fresh_fetch
motivation: 旨在探索1.58比特量化与半结构化稀疏性之间的协同效应，以进一步提升大语言模型的运行效率。
method: M稀疏化的联合应用及稳定训练。
result: 实验表明Sparse-BitNet在相同稀疏度下性能优于全精度模型，且在训练和推理阶段均获得显著加速。
conclusion: 极低比特量化与半结构化稀疏性的结合是实现高效大语言模型极具潜力的研究路径。
---

## 摘要
半结构化 N:M 稀疏和低比特量化（例如 1.58-bit BitNet）是提高大语言模型（LLMs）效率的两种极具前景的方法，但目前它们在很大程度上是独立研究的。在这项工作中，我们研究了它们的相互作用，并证明 1.58-bit BitNet 比全精度模型天然地更兼容 N:M 稀疏。为了研究这种效应，我们提出了 Sparse-BitNet，这是一个统一的框架，首次在确保训练稳定的同时，联合应用了 1.58 比特量化和动态 N:M 稀疏化。在多种模型规模和训练方案（稀疏预训练和由密到稀疏的调度）下，1.58-bit BitNet 在相同稀疏度水平下始终表现出比全精度基线更小的性能退化，并且在准确率崩溃前能够容忍更高的结构化稀疏度。此外，利用我们定制的稀疏张量核心，Sparse-BitNet 在训练和推理方面都实现了显著的加速，最高可达 1.30 倍。这些结果表明，将极低比特量化与半结构化 N:M 稀疏相结合是实现高效 LLM 的一个有前景的方向。代码可在 https://github.com/AAzdi/Sparse-BitNet 获取。

## Abstract
Semi-structured N:M sparsity and low-bit quantization (e.g., 1.58-bit BitNet) are two promising approaches for improving the efficiency of large language models (LLMs), yet they have largely been studied in isolation. In this work, we investigate their interaction and show that 1.58-bit BitNet is naturally more compatible with N:M sparsity than full-precision models. To study this effect, we propose Sparse-BitNet, a unified framework that jointly applies 1.58-bit quantization and dynamic N:M sparsification while ensuring stable training for the first time. Across multiple model scales and training regimes (sparse pretraining and dense-to-sparse schedules), 1.58-bit BitNet consistently exhibits smaller performance degradation than full-precision baselines at the same sparsity levels and can tolerate higher structured sparsity before accuracy collapse. Moreover, using our custom sparse tensor core, Sparse-BitNet achieves substantial speedups in both training and inference, reaching up to 1.30X. These results highlight that combining extremely low-bit quantization with semi-structured N:M sparsity is a promising direction for efficient LLMs. Code available at https://github.com/AAzdi/Sparse-BitNet

---

## 论文详细总结（自动生成）

以下是对论文《Sparse-BitNet: 1.58-bit LLMs are Naturally Friendly to Semi-Structured Sparsity》的结构化深入总结：

### 1. 核心问题与研究动机
*   **研究背景**：大语言模型（LLM）的规模增长带来了巨大的计算和存储开销。目前提升效率的两大主流技术是**低比特量化**（如 1.58-bit BitNet）和**半结构化 N:M 稀疏**（如 NVIDIA 支持的 2:4 模式）。
*   **核心问题**：尽管两者都能提升效率，但过去通常被独立研究。论文观察到 1.58-bit BitNet 在预训练后天然存在约 42% 的零值权重，这暗示了极低比特量化与稀疏性之间可能存在协同效应。
*   **研究动机**：探索 1.58-bit BitNet 是否比全精度（BF16）模型更适配 N:M 稀疏约束，并提出一个能同时实现超低比特量化与动态稀疏化的统一框架。

### 2. 方法论：Sparse-BitNet 框架
*   **核心思想**：在 BitNet 的训练过程中引入动态的 N:M 稀疏约束，通过“先量化后掩码”（Quant-and-Mask）的策略，确保模型在推理时既是三值化的（{-1, 0, 1}），又符合硬件加速的稀疏模式。
*   **关键技术细节**：
    *   **Sparse-BitLinear 层**：替代标准线性层。它维护一个高精度的“主权重”（Master Weights），在每一轮前向传播中动态生成稀疏掩码。
    *   **基于幅度的掩码生成**：掩码 $M_{N:M}$ 直接从连续的主权重中计算（取每 $M$ 个元素中绝对值最大的 $N$ 个），以避免三值化权重中大量“平局”导致的排序不稳定。
    *   **双重直通估计器（Dual STE）**：在反向传播时，梯度不仅穿透量化函数，还穿透稀疏掩码。这意味着即使是被掩码掉（置零）的权重也能接收梯度更新，从而允许模型在训练过程中动态调整稀疏拓扑结构。
    *   **训练策略**：采用从头开始（From-scratch）的稀疏预训练，而非事后剪枝。

### 3. 实验设计
*   **模型与规模**：基于 Qwen-2.5 架构，涵盖 0.5B、1.5B 和 3B 三种参数规模。
*   **数据集**：使用 RefineWeb 数据集，每个模型训练约 500 亿（50B）个 Token。
*   **基准测试（Benchmarks）**：
    *   **语言建模**：验证集困惑度（PPL）。
    *   **下游任务**：HellaSwag、ARC-E、PIQA、BoolQ、COPA（零样本准确率）。
*   **对比方法**：
    *   Dense BF16（全精度密集模型）
    *   Sparse BF16（6:8 稀疏全精度模型）
    *   Dense BitNet（1.58-bit 密集模型）
    *   **Sparse-BitNet**（1.58-bit + 6:8 稀疏模型，本文方法）

### 4. 资源与算力
*   **硬件设备**：性能评估使用了 NVIDIA A100 GPU（用于 Prefill 阶段测试）和 B200 GPU（用于 Decode 阶段测试）。
*   **训练规模**：每个模型规模均在 50B Token 上进行完整预训练。
*   **说明**：论文未明确给出训练这些模型所消耗的总 GPU 小时数或具体的集群规模（如 GPU 数量），但提供了详细的超参数设置（如 AdamW 优化器、学习率 1e-5 等）。

### 5. 实验数量与充分性
*   **实验维度**：
    *   **规模扩展性**：验证了从 0.5B 到 3B 的一致性表现。
    *   **稀疏度压力测试**：对 0.5B 模型进行了从 8:8（密集）到 2:8（极度稀疏）的完整扫描。
    *   **消融实验**：对比了梯度流设计（是否有全梯度）、掩码生成来源（主权重 vs 量化权重）以及量化与稀疏的先后顺序。
    *   **训练调度**：对比了“从头稀疏训练”与“由密到稀过渡训练”的效果。
*   **充分性评价**：实验设计较为全面，不仅关注准确率，还通过自定义内核测试了端到端的推理加速比，客观地展示了该方法在硬件上的实际收益。

### 6. 主要结论与发现
*   **天然适配性**：在相同的 N:M 稀疏约束下，BitNet 的性能退化远小于 BF16。例如在 2:4 稀疏下，BF16 的 PPL 飙升了 18.8%，而 BitNet 仅上升 5.7%。
*   **延迟崩溃**：BitNet 能容忍更高的稀疏度。BF16 在 4:8 模式下就开始性能崩溃，而 BitNet 直到 3:8 甚至更激进的模式下仍能保持相对稳定。
*   **极化效应（Polarization）**：分析发现，BitNet 的训练过程促使权重向两极（远离零点）分化，形成了清晰的“活跃权重”和“冗余权重”层级，这使得 N:M 剪枝更容易保留关键信息。
*   **效率提升**：结合 6:8 稀疏内核，Sparse-BitNet 在训练和推理中实现了高达 **1.30 倍** 的端到端加速。

### 7. 优点
*   **协同效应的深度挖掘**：首次系统性地证明了极低比特量化与半结构化稀疏之间的互补性。
*   **训练稳定性**：通过 Dual STE 和基于主权重的掩码生成，解决了低比特模型在动态稀疏训练中容易不稳定的难题。
*   **硬件友好**：直接面向 NVIDIA Sparse Tensor Cores 设计，具有极强的工业部署潜力。

### 8. 不足与局限
*   **模型规模上限**：实验最高仅达到 3B 参数，尚未在 70B 或更大规模的超大模型上验证该结论的普适性。
*   **任务覆盖**：主要集中在基础语言建模和常识推理，对于长文本处理、代码生成或复杂数学推理的性能影响探讨较少。
*   **绝对性能差距**：虽然 BitNet 对稀疏更鲁棒，但其绝对 PPL 仍略逊于同规模的 BF16 模型（这是 1.58-bit 量化本身的固有挑战，而非稀疏化引入的问题）。

（完）
