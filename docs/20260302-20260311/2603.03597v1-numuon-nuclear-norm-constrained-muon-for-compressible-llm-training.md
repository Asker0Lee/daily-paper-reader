---
title: "NuMuon: Nuclear-Norm-Constrained Muon for Compressible LLM Training"
title_zh: NuMuon：用于可压缩大语言模型训练的核范数约束 Muon
authors: "Hadi Mohaghegh Dolatabadi, Thalaiyasingam Ajanthan, Sameera Ramasinghe, Chamin P Hewa Koneputugodage, Shamane Siriwardhana, Violetta Shevchenko, Karol Pajak, James Snewin, Gil Avraham, Alexander Long"
date: 2026-03-04
pdf: "https://arxiv.org/pdf/2603.03597v1"
tags: ["query:llm-cp"]
score: 9.0
evidence: 通过低秩结构实现可压缩的大模型训练
tldr: 针对大语言模型部署成本高的问题，本文研究了Muon优化器的权重结构，发现其虽采用全秩更新但仍具有低秩特性。据此提出NuMuon，通过在更新方向中引入核范数约束，进一步强化权重的低秩结构。在十亿参数级模型上的实验表明，NuMuon在保持收敛性能的同时，显著提升了权重的可压缩性及压缩后的模型质量，为高效LLM训练与部署提供了新思路。
selection_source: fresh_fetch
motivation: 发现Muon优化器训练的模型天然具有低秩结构，因此希望通过显式约束进一步增强这种特性以利于模型压缩。
method: 在Muon优化器的更新方向中引入核范数约束，从而在训练过程中诱导权重矩阵形成更显著的低秩结构。
result: 在十亿参数规模的模型上，NuMuon在维持良好收敛性的同时，显著提升了权重的可压缩性及压缩后的模型性能。
conclusion: NuMuon通过核范数约束成功促进了权重的低秩化，是一种能在训练阶段预先优化模型可压缩性的有效方法。
---

## 摘要
大语言模型（LLMs）的快速进展日益受到内存和部署成本的限制，这促使了面向实际部署的压缩方法的发展。许多最先进的压缩流水线利用了训练后权重矩阵的低秩结构，这一现象通常与 Adam 等常用优化器的特性有关。在此背景下，Muon 是最近提出的一种优化器，它通过全秩更新步骤改进了 LLM 的预训练，但其诱导的权重空间结构尚未得到表征。在这项工作中，我们报告了一个令人惊讶的实证发现：尽管实施了全秩更新，但经 Muon 训练的模型在其权重矩阵中表现出显著的低秩结构，并且在标准流水线下易于压缩。受此启发，我们提出了 NuMuon，它通过在更新方向上增加核范数约束来增强 Muon，从而进一步约束学习到的权重趋向低秩结构。在十亿参数规模的模型上，我们证明了 NuMuon 在保持 Muon 良好的收敛行为的同时，提高了权重的可压缩性，并提升了在最先进 LLM 压缩流水线下压缩后的模型质量。

## 速览
**TLDR**：针对大语言模型部署成本高的问题，本文研究了Muon优化器的权重结构，发现其虽采用全秩更新但仍具有低秩特性。基于此，作者提出NuMuon，通过在更新方向上引入核范数约束，进一步强化权重的低秩结构。在十亿参数规模的模型实验中，NuMuon在保持Muon收敛优势的同时，显著提升了权重的可压缩性及压缩后的模型质量，为高效部署提供了新方案。 \
**Motivation**：Muon优化器训练的模型虽表现出低秩结构，但仍需专门的约束来进一步增强其在标准压缩流程下的表现。 \
**Method**：提出NuMuon优化器，通过在Muon的更新方向上增加核范数约束，引导权重矩阵向低秩结构演化。 \
**Result**：在十亿参数级模型上，NuMuon在维持良好收敛性的同时，显著提升了权重的可压缩性及压缩后的模型性能。 \
**Conclusion**：NuMuon证明了通过在训练阶段引入特定约束，可以有效提升大语言模型的可压缩性，助力低成本部署。

---

## Abstract
The rapid progress of large language models (LLMs) is increasingly constrained by memory and deployment costs, motivating compression methods for practical deployment. Many state-of-the-art compression pipelines leverage the low-rank structure of trained weight matrices, a phenomenon often associated with the properties of popular optimizers such as Adam. In this context, Muon is a recently proposed optimizer that improves LLM pretraining via full-rank update steps, but its induced weight-space structure has not been characterized yet. In this work, we report a surprising empirical finding: despite imposing full-rank updates, Muon-trained models exhibit pronounced low-rank structure in their weight matrices and are readily compressible under standard pipelines. Motivated by this insight, we propose NuMuon, which augments Muon with a nuclear-norm constraint on the update direction, further constraining the learned weights toward low-rank structure. Across billion-parameter-scale models, we show that NuMuon increases weight compressibility and improves post-compression model quality under state-of-the-art LLM compression pipelines while retaining Muon's favorable convergence behavior.

---

## 论文详细总结（自动生成）

这是一份关于论文《NuMuon: Nuclear-Norm-Constrained Muon for Compressible LLM Training》的结构化深度总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **研究动机**：大语言模型（LLM）的部署受限于巨大的内存和计算成本，因此模型压缩（如低秩分解）至关重要。
*   **背景**：最近出现的 **Muon** 优化器通过正交化更新显著提升了预训练效率，但其对权重矩阵结构的影响尚不明确。
*   **核心发现**：作者意外发现，尽管 Muon 采用全秩（Full-rank）更新，但其训练出的模型天然具有显著的低秩结构。
*   **核心问题**：能否通过在训练阶段显式引入秩约束，进一步增强这种低秩特性，从而在不损失性能的前提下，使模型在后续压缩（如 SVD 分解）中表现更好？

### 2. 方法论：NuMuon 优化器
*   **核心思想**：将 Muon 的正交化更新视为在光谱范数球上的线性最小化算子（LMO），并在此基础上增加一个**核范数（Nuclear-norm）约束**，作为控制更新秩的凸代理指标。
*   **关键技术细节**：
    *   **LMO 问题的转化**：证明了在核范数约束下的更新方向选择可以简化为奇异值上的线性规划问题。
    *   **闭式解（Top-k SVD）**：推导出该问题的最优解即为动量矩阵的前 $k$ 个奇异向量。这意味着 NuMuon 实际上是在做秩受限的正交更新。
    *   **高效计算**：为了避免全量 SVD 的高昂代价，采用了**随机块 Krylov 方法（Randomized Block Krylov Method）**来近似计算 Top-k 奇异向量。
    *   **秩调度器（Rank Scheduler）**：引入了随训练步数变化的秩比例策略（如 Cosine 或 Piecewise 调度），允许模型在训练初期使用较高秩更新以保证收敛，后期转为低秩更新以诱导结构。
*   **收敛保证**：论文提供了非凸假设下的收敛证明，展示了 NuMuon 在核范数意义下的平稳性界限。

### 3. 实验设计
*   **模型架构**：Qwen3-0.6B、Olmo2-1.4B、Llama3-1.8B。
*   **数据集**：使用 FineWeb-EDU 进行预训练。
*   **Benchmark**：
    *   **语言建模**：WikiText2 验证集困惑度（PPL）。
    *   **下游任务**：ARC-Easy/Challenge、HellaSwag、LAMBADA、OpenbookQA、PIQA、Winogrande。
*   **对比方法**：
    *   **优化器对比**：AdamW、原始 Muon。
    *   **压缩流水线对比**：ASVD、SVD-LLM（含 Whitening 和 LoRA 变体）、Dobi-SVD。
*   **压缩率范围**：测试了从 20% 到 80% 的不同压缩比例。

### 4. 资源与算力
*   **硬件环境**：使用了 8 张 **NVIDIA A100-SXM4-40GB** GPU 组成的集群。
*   **训练细节**：
    *   Qwen3-0.6B 训练了 8000 步（约 16.8B tokens）。
    *   Olmo2-1.4B 训练了 16000 步。
    *   Llama3-1.8B 训练了 20000 步。
    *   序列长度统一为 2048。
*   **效率说明**：NuMuon 的单步训练时间略慢于 Muon（由于 Top-k SVD 计算），但 GPU 内存占用与 Muon 接近，且均低于 AdamW。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了三个不同家族、不同规模（0.6B-1.8B）的模型，进行了跨越多种压缩算法和多个压缩率的详尽测试。
*   **消融实验**：针对秩预算（Rank Budget）、秩调度器类型（Fixed, Cosine, Piecewise）以及更新-权重子空间对齐情况进行了深入分析。
*   **客观性**：实验不仅关注训练损失，还通过推理吞吐量（Throughput）与 PPL 的权衡曲线展示了实际部署价值。实验设计合理，对比基准（AdamW 和 Muon）均采用了当前最先进的训练配置（如 WSD 调度器）。

### 6. 主要结论与发现
*   **压缩性能飞跃**：在 40%-80% 的高压缩率下，NuMuon 训练的模型在 PPL 和下游任务准确率上显著优于 Muon 和 AdamW。例如，在 Llama3-1.8B 上，NuMuon 相比 Muon 在压缩后的下游任务平均表现提升了高达 **55.8%**。
*   **低秩诱导能力**：NuMuon 显著降低了权重矩阵的稳定秩（Stable Rank），且其更新方向与权重的顶级光谱子空间保持了更好的对齐。
*   **收敛性保持**：NuMuon 在预训练阶段的收敛曲线与原始 Muon 非常接近，证明了引入核范数约束不会损害基础模型的优化质量。

### 7. 优点
*   **理论与实践结合**：将优化理论（LMO/核范数）与实际的 LLM 压缩需求紧密结合，提供了坚实的数学基础。
*   **部署友好**：直接针对“训练后即压缩”的场景，通过优化器层面的改进，降低了对复杂后处理或大规模微调的依赖。
*   **即插即用**：作为一种优化器变体，它可以轻松集成到现有的预训练流水线中。

### 8. 不足与局限
*   **模型规模限制**：实验主要集中在 2B 参数以下的模型，尚未在 7B、70B 或更大规模的模型上验证其扩展性。
*   **计算开销**：虽然使用了近似 SVD，但动态计算 Top-k 奇异向量仍比 AdamW 或原始 Muon（使用 Newton-Schulz 迭代）带来额外的计算负担。
*   **超参数敏感性**：秩调度器的选择（如初始秩、最终秩、退火步数）可能需要针对不同模型进行精细调优。

（完）
