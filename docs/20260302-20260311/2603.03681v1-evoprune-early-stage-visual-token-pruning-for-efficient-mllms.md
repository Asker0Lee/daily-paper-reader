---
title: "EvoPrune: Early-Stage Visual Token Pruning for Efficient MLLMs"
title_zh: EvoPrune：面向高效多模态大语言模型的早期视觉 Token 剪枝
authors: "Yuhao Chen, Bin Shan, Xin Ye, Cheng Chen"
date: 2026-03-04
pdf: "https://arxiv.org/pdf/2603.03681v1"
tags: ["query:llm-cp"]
score: 9.0
evidence: 针对高效多模态大模型的前期视觉Token剪枝
tldr: 多模态大语言模型（MLLM）在处理高分辨率图像和视频时面临视觉标记过多导致的推理效率瓶颈。本文提出EvoPrune，一种在视觉编码阶段进行早期剪枝的方法。通过在编码层中结合标记相似度、多样性和注意力重要性进行层级剪枝，EvoPrune在保持性能的同时显著降低了计算开销，在VideoMME等基准上实现了2倍加速且性能损失极小。
selection_source: fresh_fetch
motivation: 现有的视觉标记剪枝方法多在编码后进行，忽略了视觉编码过程中产生的巨大计算成本。
method: 提出一种在视觉编码过程中执行的层级剪枝策略，利用标记相似度、多样性和注意力权重来保留最具信息量的视觉标记。
result: "在VideoMME基准测试中，该方法在性能下降不到1%的情况下实现了2倍的推理加速。"
conclusion: EvoPrune证明了早期视觉标记剪枝在提升多模态大模型推理效率方面的巨大潜力，尤其适用于延迟敏感的部署场景。
---

## 摘要
多模态大语言模型 (MLLMs) 在视觉语言任务中表现出强大的性能，但其推理效率受到高分辨率图像和视频等复杂场景中视觉 Token 指数级增长的严重限制。现有的视觉 Token 剪枝方法主要在视觉编码之后进行，忽略了编码阶段产生的巨大计算开销。为了解决这一问题，我们提出了 EvoPrune，这是一种用于 MLLMs 的早期视觉 Token 剪枝方法，直接在视觉编码过程中执行剪枝。具体而言，EvoPrune 采用了一种由 Token 相似度、多样性和基于注意力的重要性引导的逐层剪枝策略，以在选定的编码层保留最具信息量的视觉 Token。在图像和视频基准测试上的广泛实验验证了 EvoPrune 的有效性。特别是在 VideoMME 数据集上，EvoPrune 在性能下降不到 1% 的情况下实现了 2 倍的推理加速，展示了其在延迟敏感型 MLLM 部署中的潜力。

## 速览
**TLDR**：多模态大语言模型（MLLM）在处理高分辨率图像和视频时面临视觉标记过多导致的推理效率瓶颈。本文提出EvoPrune，一种在视觉编码阶段进行早期剪枝的方法。通过在编码层中结合标记相似度、多样性和注意力重要性进行层级剪枝，EvoPrune在保持性能的同时显著降低了计算开销，在VideoMME等基准上实现了2倍加速且性能损失极小。 \
**Motivation**：现有的视觉标记剪枝方法多在编码后进行，忽略了视觉编码过程中产生的巨大计算成本。 \
**Method**：提出一种在视觉编码过程中执行的层级剪枝策略，利用标记相似度、多样性和注意力权重来保留最具信息量的视觉标记。 \
**Result**：在VideoMME基准测试中，该方法在性能下降不到1%的情况下实现了2倍的推理加速。 \
**Conclusion**：EvoPrune证明了早期视觉标记剪枝在提升多模态大模型推理效率方面的巨大潜力，尤其适用于延迟敏感的部署场景。

---

## Abstract
Multimodal Large Language Models (MLLMs) have shown strong performance in vision-language tasks, but their inference efficiency is severely limited by the exponential growth of visual tokens in complex scenarios such as high-resolution images and videos. Existing visual token pruning methods mainly operate after visual encoding, overlooking the substantial computational cost incurred during the encoding stage. To address this issue, we propose EvoPrune, an early-stage visual token pruning method for MLLMs that performs pruning directly during visual encoding. Specifically, EvoPrune employs a layer-wise pruning strategy guided by token similarity, diversity, and attention-based importance to retain the most informative visual tokens at selected encoding layers. Extensive experiments on image and video benchmarks validate the effectiveness of EvoPrune. In particular, on the VideoMME dataset, EvoPrune achieves 2$\times$ inference speedup with less than 1% performance degradation, demonstrating its potential for latency-sensitive MLLM deployment.

---

## 论文详细总结（自动生成）

### 论文总结：EvoPrune - 面向高效 MLLM 的早期视觉 Token 剪枝

#### 1. 核心问题与整体含义（研究动机和背景）
多模态大语言模型（MLLM）在处理高分辨率图像和长视频时，视觉 Token 的数量呈指数级增长，导致推理效率低下。现有的视觉 Token 剪枝方法（如基于注意力或相似度的筛选）大多在**视觉编码器之后**或在 **LLM 内部**执行。
*   **痛点**：随着输入规模增加（如从 1 帧增加到 64 帧），视觉编码器的计算开销逐渐占据主导地位。现有方法由于剪枝滞后，无法缓解编码阶段的巨大计算瓶颈。
*   **目标**：提出一种“早期剪枝”框架 **EvoPrune**，直接在视觉编码过程中逐步合并冗余 Token，从而实现从编码器到 LLM 的全流程端到端加速。

#### 2. 方法论：核心思想与关键技术
EvoPrune 的核心思想是在视觉编码器的特定层中，通过**多准则引导的 Token 合并**策略，动态减少 Token 数量。
*   **逐层剪枝预算分配**：通过预定义的策略（如 Skip 策略，即每隔一层进行一次剪枝）将总剪枝预算分配到编码器的不同层。
*   **得分引导的 Token 合并 (Score-Guided Merging)**：
    *   **相似度吸引 (Similarity Attraction)**：计算 Token 间的余弦相似度，鼓励合并语义相近的 Token。
    *   **多样性惩罚 (Diversity Penalty)**：利用高斯核估计 Token 的局部密度，位于稀疏区域（独特性高）的 Token 会被赋予较低的合并权重，从而保留视觉多样性。
    *   **注意力保留 (Attention Preservation)**：计算 Token 的平均注意力权重，识别出对下游推理至关重要的“关键 Token”，并强制禁止合并这些 Token（Critical Token Ratio）。
*   **算法流程**：在选定的编码层，将 Token 集分为两组，计算跨组的综合得分矩阵，选取得分最高的 Top-r 对进行合并，并更新 Token 集合进入下一层。

#### 3. 实验设计
*   **数据集与场景**：
    *   **图像理解**：VQAv2、MME、MMBench (EN/CN)、MMVet。
    *   **视频理解**：MVBench、LongVideoBench、Video-MME（涵盖短、中、长视频）。
*   **Benchmark 与基座模型**：
    *   图像任务使用 **LLaVA-1.5-7B**；视频任务使用 **LLaVA-Video-7B**（基于 Qwen2）。
*   **对比方法**：对比了 5 种 SOTA 剪枝方法：FasterVLM（基于注意力）、VisPruner（注意力+相似度）、DART/DivPrune（基于相似度）、CDPruner（基于查询+相似度）。

#### 4. 资源与算力
*   **硬件环境**：实验均在 **NVIDIA H20 GPU** 上进行。
*   **训练/推理时长**：论文未明确提及训练时长，因为 EvoPrune 是一种**无需重新训练（Training-free）**的即插即用方法。实验重点在于推理阶段的延迟（Latency）和首字生成时间（TTFT）的缩减。

#### 5. 实验数量与充分性
*   **实验规模**：论文在 8 个主流图像和视频基准测试上进行了广泛评估，涵盖了从 77.8% 到 94.4% 的不同剪枝率。
*   **消融实验**：专门针对“注意力保留”和“多样性惩罚”两个组件进行了消融分析，验证了多准则融合的必要性。
*   **策略分析**：对比了 6 种不同的层级预算分配策略（Mean, Skip, First, Last, Increasing, Decreasing），最终确定 Skip 策略为最优。
*   **评价**：实验设计全面，对比了多种不同原理的 SOTA 方法，且涵盖了从静态图像到长视频的多种模态，实验结果具有较强的说服力和客观性。

#### 6. 主要结论与发现
*   **显著加速**：在 Video-MME 基准上，EvoPrune 实现了 **2 倍的推理加速**，而性能下降不到 1%。
*   **全流程优化**：相比于仅优化 LLM 阶段的传统方法，EvoPrune 将视觉编码器的耗时减少了约 45%，中间处理环节加速了 5 倍。
*   **性能领先**：在相同的 Token 保留率下，EvoPrune 的平均准确率始终优于 CDPruner 和 VisPruner 等竞争对手，尤其在极端剪枝（如仅保留 32 个 Token）的情况下表现更稳健。

#### 7. 优点与亮点
*   **早期介入**：填补了现有研究对“编码器计算开销”关注不足的空白，实现了真正的端到端加速。
*   **即插即用**：无需对模型进行微调或重训，易于集成到现有的 MLLM 架构中。
*   **多准则平衡**：通过引入多样性惩罚和注意力保护，有效解决了纯相似度合并容易丢失细粒度信息的问题。

#### 8. 不足与局限
*   **时域冗余挖掘有限**：虽然在视频任务表现优异，但目前主要侧重于空间和语义维度的合并，对跨帧（Temporal）的深度冗余挖掘（如关键帧动态采样）尚有提升空间。
*   **超参数敏感性**：Critical Token Ratio (CTR) 和层级分配策略对不同模型可能需要微调以达到最优平衡。
*   **应用限制**：主要针对 Transformer 架构的视觉编码器，对于其他类型的编码器（如纯 CNN）可能不直接适用。

（完）
