---
title: "MASQuant: Modality-Aware Smoothing Quantization for Multimodal Large Language Models"
title_zh: MASQuant：面向多模态大语言模型的模态感知平滑量化
authors: "Lulu Hu, Wenhu Xiao, Xin Chen, Xinhua Xu, Bowen Xu, Kun Li, Yongliang Tao"
date: 2026-03-05
pdf: "https://arxiv.org/pdf/2603.04800v1"
tags: ["query:llm-cp"]
score: 10.0
evidence: 针对多模态大模型的模态感知平滑量化
tldr: 针对多模态大语言模型（MLLM）在量化过程中存在的平滑失调和跨模态计算不变性挑战，本文提出了MASQuant框架。该框架引入了模态感知平滑（MAS）以学习特定模态的平滑因子，并利用跨模态补偿（CMC）通过SVD白化技术处理激活差异，从而实现跨模态的统一量化。实验证明，MASQuant在双模态和三模态模型上均表现出稳定的量化性能，达到了当前最先进的水平。
selection_source: fresh_fetch
motivation: 现有的LLM量化方法在处理多模态模型时，难以应对不同模态间的平滑失调和计算不变性挑战。
method: 提出MASQuant框架，通过模态感知平滑（MAS）和基于SVD白化的跨模态补偿（CMC）来优化量化过程。
result: 实验结果显示MASQuant在多种多模态模型上均取得了与SOTA算法相当的竞争性表现。
conclusion: MASQuant为多模态大模型提供了一种有效且通用的训练后量化（PTQ）解决方案。
---

## 摘要
具有计算不变性的大语言模型（LLMs）训练后量化（PTQ）已取得显著进展，然而，将其应用于多模态大语言模型（MLLMs）仍面临巨大挑战。在本文中，我们以 SmoothQuant 为案例进行分析，并识别出两个关键问题：平滑失配（Smoothing Misalignment）和跨模态计算不变性（Cross-Modal Computational Invariance）。为了解决这些问题，我们提出了模态感知平滑量化（MASQuant），这是一个新颖的框架，它引入了：（1）模态感知平滑（MAS），通过学习独立的、特定于模态的平滑因子来防止平滑失配；（2）跨模态补偿（CMC），通过使用 SVD 白化将多模态激活差异转换为低秩形式，从而实现跨模态的统一量化，以此解决跨模态计算不变性问题。MASQuant 在双模态和三模态 MLLMs 上均表现出稳定的量化性能。实验结果表明，MASQuant 在最先进的 PTQ 算法中具有竞争力。源代码：https://github.com/alibaba/EfficientAI。

## Abstract
Post-training quantization (PTQ) with computational invariance for Large Language Models~(LLMs) have demonstrated remarkable advances, however, their application to Multimodal Large Language Models~(MLLMs) presents substantial challenges. In this paper, we analyze SmoothQuant as a case study and identify two critical issues: Smoothing Misalignment and Cross-Modal Computational Invariance. To address these issues, we propose Modality-Aware Smoothing Quantization (MASQuant), a novel framework that introduces (1) Modality-Aware Smoothing (MAS), which learns separate, modality-specific smoothing factors to prevent Smoothing Misalignment, and (2) Cross-Modal Compensation (CMC), which addresses Cross-modal Computational Invariance by using SVD whitening to transform multi-modal activation differences into low-rank forms, enabling unified quantization across modalities. MASQuant demonstrates stable quantization performance across both dual-modal and tri-modal MLLMs. Experimental results show that MASQuant is competitive among the state-of-the-art PTQ algorithms. Source code: https://github.com/alibaba/EfficientAI.

---

## 论文详细总结（自动生成）

以下是对论文《MASQuant: Modality-Aware Smoothing Quantization for Multimodal Large Language Models》的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：现有的训练后量化（PTQ）方法（如 SmoothQuant）在应用于多模态大语言模型（MLLM）时效果不佳。
*   **研究动机**：
    *   **平滑失调（Smoothing Misalignment）**：不同模态（文本、图像、音频）的激活值量级差异巨大（视觉激活通常比文本/音频大 10-100 倍）。传统方法对所有模态使用统一的平滑因子，导致主导模态（通常是视觉）决定了平滑参数，而弱势模态被“过度平滑”，信号被破坏。
    *   **跨模态计算不变性挑战**：如果为每个模态存储独立的量化权重，会违背量化减少内存占用的初衷。如何在保持单一量化权重的前提下，实现模态特定的优化是关键。

### 2. 方法论
MASQuant 框架由两个核心组件组成：
*   **模态感知平滑（MAS）**：
    *   不再寻找单一的超参数 $\beta$，而是直接为每个模态 $m$ 优化独立的平滑因子矩阵 $S_m$。
    *   通过最小化模态特定的平均绝对误差（MAE）损失来学习这些因子，使每个模态的通道间离群值都能得到针对性抑制。
*   **跨模态补偿（CMC）**：
    *   **核心思想**：以文本模态的平滑权重 $Q(S_t W)$ 作为基础权重。对于其他模态，通过轻量级的低秩矩阵进行补偿。
    *   **关键技术**：论文证明了跨模态的激活差异在经过 **SVD 白化（Whitening）** 处理后具有显著的低秩特性。
    *   **流程**：利用 SVD 将白化后的残差分解为两个低秩矩阵 $L_1$ 和 $L_2$。推理时，基础路径执行标准量化矩阵乘法，补偿路径执行低秩运算，最后求和：$Y = Q(X_m S_m^{-1}) \cdot Q(S_t W) + X_m S_m^{-1} \cdot L_1 L_2$。

### 3. 实验设计
*   **模型对象**：Qwen2.5-VL（3B, 7B）和 Qwen2.5-Omni（3B, 7B，支持音视频文三模态）。
*   **数据集/场景**：
    *   **视觉推理**：MMMU, OCRBench, VizWiz, ScienceQA, TextVQA。
    *   **音频任务**：Librispeech, Wenetspeech。
    *   **全模态推理**：OmniBench。
*   **对比方法（Benchmark）**：RTN（四舍五入量化）、AWQ、SmoothQuant (SQ)、MBQ（模态平衡量化）。
*   **量化配置**：涵盖 W4A16、W8A8、W4A8、W4A6 等多种位宽组合。

### 4. 资源与算力
*   **算力设备**：文中提到在 **Desktop RTX 4090** 上测试了推理加速性能。
*   **训练/校准细节**：
    *   使用了融合 GPU 算子（基于 Nunchaku）来验证预填充阶段的提速。
    *   校准过程通常只需要少量样本（如分析图中提到的 32 个样本），符合 PTQ 的高效特征。
    *   未明确说明完整校准过程的总 GPU 小时数，但提到 MAS 优化仅需 2 个 Epoch 即可收敛。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了双模态（VL）和三模态（Omni）模型，并在 3B 和 7B 两个参数量级上进行了验证。
*   **消融实验**：非常充分，包括：
    *   不同模态损失权重（$\lambda$）的影响。
    *   训练轮数（Epochs）对性能的提升。
    *   MAS 和 CMC 各自的贡献分析。
    *   低秩矩阵秩的大小（Rank Ratio）与信号量化噪声比（SQNR）的关系。
*   **客观性**：通过多维度指标（准确率、词错率 WER、困惑度 PPL）进行评估，对比了当前主流的 MLLM 量化 SOTA 方法，实验设计较为公正。

### 6. 主要结论与发现
*   **解决崩溃问题**：在 W4A8 等低位宽下，传统方法会导致音频模态性能彻底崩溃（WER 飙升 20 倍），而 MASQuant 能保持接近 FP16 的水平。
*   **性能领先**：在所有评估的基准测试中，MASQuant 始终优于现有的通道平滑 PTQ 方法。
*   **高效推理**：在 RTX 4090 上，MASQuant 相比 FP16 实现了约 **2.5 倍** 的加速，且相比基础量化方法仅引入了极小的延迟开销（5-10%）。

### 7. 优点
*   **理论支撑**：通过数学证明了白化操作能诱导跨模态差异呈现低秩结构，为 CMC 提供了理论依据。
*   **针对性强**：精准识别并解决了 MLLM 中特有的“模态主导”导致的平滑失调问题。
*   **通用性**：不仅适用于视觉-文本，也能无缝扩展到音频等更多模态。
*   **工程落地**：提供了定制化的 CUDA 内核实现，证明了算法在实际部署中的可行性。

### 8. 不足与局限
*   **额外存储**：虽然补偿矩阵是低秩的，但相比纯量化模型，仍需额外存储 $L_1, L_2$ 矩阵（尽管内存占比很小）。
*   **校准依赖**：作为 PTQ 方法，其性能高度依赖于校准数据的模态覆盖范围和代表性。
*   **组件限制**：实验主要集中在 LLM 解码器部分（Thinker），未深入探讨视觉/音频编码器（Encoder）在极端量化下的表现。

（完）
