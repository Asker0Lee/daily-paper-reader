---
title: "The Model Knows Which Tokens Matter: Automatic Token Selection via Noise Gating"
title_zh: 模型知道哪些 Token 重要：通过噪声门控实现自动 Token 选择
authors: "Landi He, Xiaoyu Yang, Lijian Xu"
date: 2026-03-07
pdf: "https://arxiv.org/pdf/2603.07135v1"
tags: ["query:llm-cp"]
score: 9.0
evidence: 通过噪声门控为视觉语言模型进行自动Token选择剪枝
tldr: "针对视觉语言模型（VLM）中视觉标记冗余导致的推理开销问题，本文提出 AutoSelect 自动标记选择方法。该方法将标记剪枝建模为受限带宽下的信息传输，通过轻量级评分器和噪声门控机制，在仅使用标准预测损失的情况下训练模型识别重要标记。AutoSelect 在推理时仅保留 Top-K 标记，在保持 96.5% 准确率的同时，将 LLM 预填充速度提升 2.85 倍，且具有极低的额外开销和良好的跨模型迁移性。"
selection_source: fresh_fetch
motivation: 视觉标记在 VLM 推理中占据主要成本且存在大量冗余，而现有剪枝方法往往依赖启发式指标且缺乏对信息流的直接建模。
method: 提出 AutoSelect 框架，通过在冻结的 VLM 中引入轻量级评分器和噪声门控，利用方差保持的噪声调制信息流，并结合去噪器在训练中学习标记重要性。
result: "在 10 个基准测试中，该方法在仅增加 0.69ms 开销的情况下，实现了 2.85 倍的预填充加速，并保留了原模型 96.5% 的性能。"
conclusion: AutoSelect 证明了模型能够自主识别关键标记，为高效 VLM 推理提供了一种无需辅助目标、低开销且通用的标记选择方案。
---

## 摘要
在视觉语言模型（VLM）中，视觉 token 占据了主要的推理成本，但其中许多包含冗余信息。现有的剪枝方法缓解了这一问题，但通常依赖于注意力权重大小或相似度得分。我们将视觉 token 剪枝重新定义为容量受限的通信问题：在给定固定预算 K 的情况下，模型必须分配有限的带宽以最大限度地保留视觉信息。我们提出了 AutoSelect，它在冻结的 VLM 上附加了一个轻量级的评分器（Scorer）和去噪器（Denoiser），且仅使用标准的下一个 token 预测损失进行训练，无需辅助目标或额外标注。在训练期间，一个方差保持噪声门控根据预测的重要性调节每个 token 的信息流，从而使梯度能够传播到所有 token；随后，一个对角注意力去噪器恢复受扰动的表示。在推理时，仅保留评分器和硬 top-K 选择，增加的延迟微乎其微。在十个 VLM 基准测试中，AutoSelect 在仅增加 0.69 毫秒开销的情况下，保留了完整模型 96.5% 的准确率，同时将 LLM 预填充速度提升了 2.85 倍，并且可以迁移到不同的 VLM 骨干网络，无需针对特定架构进行调整。代码可在 https://github.com/MedHK23/AutoSelect 获取。

## Abstract
Visual tokens dominate inference cost in vision-language models (VLMs), yet many carry redundant information. Existing pruning methods alleviate this but typically rely on attention magnitude or similarity scores. We reformulate visual token pruning as capacity constrained communication: given a fixed budget K, the model must allocate limited bandwidth to maximally preserve visual information. We propose AutoSelect, which attaches a lightweight Scorer and Denoiser to a frozen VLM and trains with only the standard next token prediction loss, without auxiliary objectives or extra annotations. During training, a variance preserving noise gate modulates each token's information flow according to its predicted importance so that gradients propagate through all tokens; a diagonal attention Denoiser then recovers the perturbed representations. At inference, only the Scorer and a hard top-K selection remain, adding negligible latency. On ten VLM benchmarks, AutoSelect retains 96.5% of full model accuracy while accelerating LLM prefill by 2.85x with only 0.69 ms overhead, and transfers to different VLM backbones without architecture-specific tuning. Code is available at https://github.com/MedHK23/AutoSelect.

---

## 论文详细总结（自动生成）

### 论文总结：The Model Knows Which Tokens Matter: Automatic Token Selection via Noise Gating

#### 1. 核心问题与整体含义（研究动机和背景）
视觉语言模型（VLM）通常将图像编码为大量视觉标记（Tokens）输入给大语言模型（LLM）。由于自注意力机制的计算复杂度随序列长度呈平方级增长，这些视觉标记成为了推理速度和内存占用的主要瓶颈。
*   **研究动机**：现有的标记剪枝方法多依赖于注意力权重或相似度等局部启发式信号，缺乏对全局信息保留的直接优化。
*   **核心思想**：本文将视觉标记剪枝重新定义为**“容量受限的通信”**问题。即在固定带宽（Token 预算 $K$）下，通过训练模型自主学习如何分配有限的容量，以最大限度地保留对下游任务有用的视觉信息。

#### 2. 方法论：核心思想与关键技术
AutoSelect 框架在冻结的 VLM 架构中插入了两个轻量级模块：**评分器（Scorer）**和**去噪器（Denoiser）**。
*   **可微标记选择**：使用 **Soft Top-K** 算子将评分器输出的原始分数转化为准二进制的掩码 $\alpha$，通过温度退火策略在训练时保持梯度流动，在推理时收敛至硬选择。
*   **方差保持噪声门控（VP Noise Gate）**：这是训练阶段的关键。模型不直接丢弃标记，而是根据重要性分数 $\alpha$ 向每个标记注入高斯噪声：$\tilde{x}_i = \sqrt{\alpha_i} x_i + \sqrt{1 - \alpha_i} \epsilon_i$。
    *   分数高的标记保留原始信号；分数低的标记被噪声淹没。
    *   这种方式强制模型在有限的“信噪比”预算下，将高分分配给最重要的信息。
*   **对角注意力去噪器（Diagonal Attention Denoiser）**：在将受扰动的标记送入 LLM 前，使用单层 Transformer 进行去噪。
    *   **关键细节**：采用对角线注意力掩码，限制标记只能关注自身，防止重要标记的信息泄露给被噪声淹没的标记，从而确保容量约束的有效性。
*   **训练目标**：仅使用标准的**下一个标记预测（Next Token Prediction）**损失，无需任何辅助损失函数或边界框标注。

#### 3. 实验设计
*   **基准测试（Benchmarks）**：涵盖 10 个主流 VLM 评测集，包括 GQA、MMBench (CN)、MME、POPE、ScienceQA、VQAv2、TextVQA、SEED-Bench 和 VizWiz。
*   **骨干模型**：在 LLaVA-1.5-7B（基础）、LLaVA-NeXT-7B（高分辨率）和 Qwen2.5-VL-7B（变长输入/不同架构）上进行了验证。
*   **对比方法**：对比了包括训练无关方法（ToMe, FastV, DivPrune, VisionZip, HoloV）和需要训练/微调的方法（SparseVLM, DART, PDrop, PRUNESID）在内的 10 余种前沿方案。

#### 4. 资源与算力
*   **训练配置**：在 ImageNet-1K 的标题生成子集上进行训练。
*   **硬件使用**：实验在一台 **NVIDIA A6000 GPU** 上完成。
*   **参数规模**：仅训练约 **84M** 的新增参数（Scorer 和 Denoiser），VLM 的视觉编码器、投影器和 LLM 均保持冻结。
*   **推理开销**：评分器模块的额外延迟仅为 **0.69 ms**，在同类方法中处于极低水平。

#### 5. 实验数量与充分性
*   **实验规模**：论文在 3 种不同架构的模型上进行了广泛测试，涵盖了从 576 到 2880 个标记的不同输入规模。
*   **消融实验**：针对 VP 噪声门控 vs 缩放门控、对角注意力 vs 全局注意力进行了对比，验证了设计决策的必要性。
*   **公平性与客观性**：
    *   通过 **LLM-free 实验**（在 ImageNet 上直接做分类）证明了 Scorer 的提升源于视觉特征选择的质量，而非仅仅是 LLM 的适应。
    *   详细分解了预填充阶段的延迟（TTFT），确保加速比的计算真实可靠。

#### 6. 主要结论与发现
*   **高性能保留**：在 LLaVA-1.5-7B 上，剪枝掉 88.9% 的标记后，仍能保留 96.5% 的原始性能。
*   **显著加速**：在保持高精度的前提下，将 LLM 的预填充（Prefill）阶段加速了 **2.85 倍**。
*   **通用性**：AutoSelect 无需针对特定架构调整即可迁移至 Qwen2.5-VL 等新型模型，且在处理变长标记序列时表现优异。
*   **语义聚焦**：可视化显示，模型自动学会了关注目标主体（如人脸、手部），而忽略冗余的背景或相似纹理。

#### 7. 优点（亮点）
*   **端到端简洁性**：不需要复杂的辅助损失（如稀疏性损失或定位损失），仅靠生成任务即可驱动标记选择。
*   **推理零负担**：Denoiser 仅在训练时存在，推理时被完全移除，不增加计算开销。
*   **文本无关性**：Scorer 仅依赖视觉特征，这意味着在多轮对话中，视觉标记的选择可以被缓存复用，无需随每条指令重新计算。
*   **理论优雅**：将剪枝问题转化为信息论中的带宽受限通信问题，提供了清晰的解释。

#### 8. 不足与局限
*   **指令感知缺失**：由于 Scorer 是文本无关的，它可能无法根据特定问题（如“图中左上角有什么？”）动态调整关注点，虽然这换取了推理效率，但在极端细粒度任务上可能存在瓶颈。
*   **训练依赖**：虽然是轻量级训练，但仍需要一定的图像-文本对数据进行收敛，不如完全 Training-free 的方法部署快。
*   **极端剪枝下的退化**：尽管表现优于基准，但在极低预算（如仅保留 64 个标记处理复杂图像）时，性能仍有不可避免的下降。

（完）
